% 와부고 재현 2학기 중간고사 2회 · 문제지 (SIMULATOR [와부] NO.2)
% ============================================================================
% 세션 115 · 2026-08-04 · 조판 1차
%
% 🔴 디자인 단일 출처: templates/SIMULATOR-와부-디자인-v1.md
%    골든 회차: output/공통수학2/2026-2학기-와부고-재현-중간-1회/재현-1회-문제.tex
%    이 파일은 골든의 프리앰블을 그대로 쓰고 회차 표기(NO.1 → NO.2)와 본문만 갈았다.
%
% 🔴 콘텐츠 단일 출처: 이 폴더의 청사진.yaml + 제안서/ 열둘
%    슬롯마다 원본·변형·정답·검산이 제안서에 다 적혀 있다. 여기서 새로 만든 수학은 없다.
%
% 🔵🔵 2026-08-05 세션 115 — 선택형 열둘을 **맹검 실측값 순으로 다시 늘어놓았다**
%    까닭: 다섯 번째 맹검에서 옛 11번이 3.30 인데 5.4점을 달고 열한 번째에 있었다.
%          옛 4번(4.30)보다 한 점 가벼웠다. 판정자 흔들림(±0.3)을 넘는 어긋남이다.
%    정책: memory/feedback_exam_difficulty_ascending_order.md
%    🔴 문항 본문은 한 글자도 안 고쳤다. 자리와 배점만 바꿨으므로 판정값이 그대로 따라온다.
%    🔴 아래 문항 주석 안의 「N번」 은 **옛 번호**다. 대응은 이 표의 「옛」 칸을 봐라.
%       서술형 13~17 은 안 옮겼다 — 셋(14·15·16)이 다 8.0점이고 값 차이가 판정 흔들림 안이다.
%
%   #  배점  옛  단원   원본                              도구                            정답 실측
%   ---------------------------------------------------------------------------------------------
%    1  4.5   2  집합  2022 2학기 중간 1번               여집합·차집합 원소 개수          ④  2.33
%    2  4.6   1  도형  2021 1학기 중간 13번              수선 둘의 교점                  ④  2.90
%    3  4.7   5  도형  2021 1학기 중간 15번              원 위의 점에서의 접선 · 안 만남   ①  3.40
%    4  4.8  11  도형  2016 1학기 기말 8번               정점을 디딤돌로 · 거리가 최대     ④  3.30
%    5  4.9   8  명제  2025 2학기 기말 4번               「모든」은 교집합·「어떤」은 합집합 ②  3.40
%    6  5.0   3  도형  2023 1학기 기말 2번               대칭점을 감춘 최단 거리          ③  3.47
%    7  5.0   7  도형  2022 1학기 기말 2번               세 직선이 삼각형을 안 이룸       ③  3.73
%    8  5.1   6  집합  2023 2학기 중간 14번              배수 집합 → 정수 개수            ②  4.33
%    9  5.2   9  도형  2019 1학기 기말 13번              평행선이 만드는 길이비           ③  4.27
%   10  5.3   4  집합  2022 2학기 중간 11번              두 조건을 겹쳐 X⊂A · a 되짚기    ③  4.30
%   11  5.4  10  명제  2023 2학기 중간 4번               산술기하가 통하는 자리·아닌 자리 ④  4.63
%   12  5.5  12  도형  2024 1학기 기말 11번              원 하나 + 직선 둘의 교점 개수    ⑤  4.73
%   13  6.0  도형  2023 1학기 기말 8번               넓이를 조건으로 직선 되짚기      —
%   14  8.0  집합  2024 2학기 중간 17번              교집합 개수의 최대·최소로 되짚기 —
%   15  8.0  융합  2023 2학기 중간 19번 + 2024 1기말 17번  진리집합 ↔ 원과 영역       —
%   16  8.0  도형  2019 1학기 기말 17번              정사각형 · 두 축 위의 꼭짓점     —
%   17 10.0  도형  2021 1학기 중간 17번              두 점을 지나는 원 · 구간 덮기    —
%
%   선택형 정답 자리: ④ ④ ① ④ ② ③ ③ ② ③ ③ ④ ⑤
%   분포 ① 1 · ② 2 · ③ 4 · ④ 4 · ⑤ 1 · 잇달는 곳은 1~2 · 6~7 · 9~10 셋인데 모두 길이 둘이다.
%     🔴 연속 셋 이상 없음 — 통합관리 C33 통과.
%     🔵 다시 늘어놓을 때 값만 보고 줄 세우면 ④④④ 와 ③③③③ 가 생겼다. 값이 붙어 있는 자리
%        (3·4·5·6 이 3.30~3.47, 8·9·10 이 4.27~4.33)에서 순서를 바꿔 이음을 끊었다.
%        🔴 그 안에서 바꾼 것은 판정자 흔들림보다 작은 차이다 — 오름차순이 깨진 것이 아니다.
%     🟡 ③ 과 ④ 가 넷씩인 것은 그대로다. 선지가 값이라 오름차순이 자리를 정한다.
%        옮기려면 실제 오류가 아닌 오답을 지어내야 해서 두었다.
%
%   회차 분포 (🔴 결과 보고일 뿐이다 — 아래 마스터 지적 참조)
%     도형: 2023 1기말 4 · 2024 1기말 3 · 2021 1중간 3 · 2022 1기말 2
%     집합·명제: 2023 2중간 3 · 2022 2중간 2 · 2025 2기말 1 · 2019 2중간 1
%   배점 합계: 선택형 60.0 + 서술형 40.0 = 100.0
%
% 🔴🔴 마스터 지적 (2026-08-05) — 「**회차의 존재이유는 고른 회차분포가 아니야.
%    중요한거는 제작 시험지의 완성도야.**」
%    단일 출처: bank/마스터-지적/2026-08-05-회차분포는-목적이-아니다.md
%    🔴 회차 분포를 이유로 더 나은 문항을 버리지 않는다. 실제 금지는 「한 회차를 통째로 옮겨 오지 마라」 하나다.
%    2024 2학기 중간이 0문이 됐다 — 결함이 아니다. 14번이 2019 2학기 중간을 새로 연 것도 이 완화 덕이다.
%
% 🔴🔴 2026-08-05 세션 115 — 다섯 자리를 다시 짰다 (8 · 10 · 13 · 14 · 17)
%    계기: 재현-1회 17문과 이 회차 17문을 한 맹검지에 섞어 같은 판정자 셋에게 물었더니
%          **2회가 0.37 낮았다** (1회 4.06 · 2회 3.69 · 프리미엄 1 : 0 · A3 칸 6 : 3).
%          기록 bank/measurements/회차대조-집계-v13.md
%    🔴 다섯 자리의 옛 값 — 8번 2.93 · 10번 3.40 · 13번 2.70 · 14번 2.97 · 17번 4.93
%    🔴 급은 여기 적지 않는다. 다시 맹검 판정으로만 정한다.
%
% 🔴🔴 13번 발문 — 제안서와 다르다. 마스터 확정 2026-08-04 세션 115 「신발끈은 쓰지마」
%    제안서 `제안서/원본pick-제안-슬롯13-14.md`:57 은 방법을 지시하는 단서를 넣으라고 했는데
%    templates/SIMULATOR-와부-디자인-v1.md:231 이 발문 방법 지시를 금지한다. 둘이 부딪혔다.
%    🔴 푼 방법 — **방법을 지시하지 않고 구할 값을 늘렸다.** 둘 다 지킨다.
%       「두 점 B, D 에서 직선 AC 에 이르는 거리를 각각 구하고, 사각형의 넓이를 구하시오」
%    자세한 셈은 13번 자리 주석에 적었다.
%
% 🔴 그림 없음 — 열일곱 문항 어디에도 그림을 넣지 않았다.
%    13·16·17번은 제안서가 「넣으면 답이 샌다」고 못박았고 나머지는 그림 없이 뜻이 정해진다.
%    (feedback_figure_not_required_everywhere — 「그림 없음」은 결함이 아니다)
% ============================================================================

\documentclass[10pt]{article}
\usepackage{style}
\usepackage{microtype}  % \textls (letter-spacing) 지원

% ── 대타이틀 (교재명 + 종속표기) · 회차 ──
%   크기는 현재 폰트 크기(\f@size)에 비례. p1 대타이틀 32pt / p2~ 러닝헤드 \large 양쪽 대응.
\makeatletter
\renewcommand{\examround}{SIMULATOR\,{\fontsize{\the\dimexpr\f@size pt*3/5\relax}{\the\dimexpr\f@size pt*3/5\relax}\selectfont\mdseries[와부]}}
\renewcommand{\examvol}{{\fontsize{\the\dimexpr\f@size pt*11/20\relax}{\the\dimexpr\f@size pt*11/20\relax}\selectfont\mdseries NO.2}}
% p2~ 러닝헤드: 교재명 SIMULATOR 만 정확히 중앙에 두고, 종속표기 `[와부] NO.2` 는
%   \makebox[0pt][l] 로 폭 0 을 주어 중앙 정렬 계산에서 제외한다.
%   단일 출처: templates/SIMULATOR-와부-디자인-v1.md §(3b)
\renewcommand{\examrunhead}{%
  SIMULATOR\makebox[0pt][l]{\,{\fontsize{\the\dimexpr\f@size pt*3/5\relax}{\the\dimexpr\f@size pt*3/5\relax}\selectfont\mdseries [와부]\ NO.2}}}
\makeatother

% ── 완전 검정 통일 팔레트 (안 C · 예외 없음) ──
%   style.sty 원본 그대로 유지 · 이 파일에서만 vB-* HTML 값 재매핑 · 파급 0
\definecolor{vB-ink}{HTML}{000000}
\definecolor{vB-deep}{HTML}{000000}
\definecolor{vB-accent}{HTML}{000000}
\definecolor{vB-rule}{HTML}{000000}
\definecolor{vB-light}{HTML}{FFFFFF}
\definecolor{vB-dim}{HTML}{000000}
\definecolor{vB-frame}{HTML}{000000}
\definecolor{vB-warn}{HTML}{000000}

% ── 페이지 세로 분할선 · 좌표 노드 기반 픽셀 맞춤 ──
%   원리: 헤더 하단·푸터 상단 룰이 그려질 때 y좌표 기록 → 두 좌표 잇는 세로 룰
%   2-pass 필수 (첫 xelatex은 좌표 기록만, 둘째부터 실제 draw)
\usepackage{eso-pic}
\usepackage{zref-savepos}
\newcommand{\dmvlinetop}{\zsavepos{dm-vline-top-\thepage}}
\newcommand{\dmvlinebot}{\zsavepos{dm-vline-bot-\thepage}}
\AddToShipoutPictureFG{%
  \begingroup
  \edef\vlinetopsp{\zposy{dm-vline-top-\thepage}}%
  \edef\vlinebotsp{\zposy{dm-vline-bot-\thepage}}%
  \ifnum\vlinetopsp>0 %
    \ifnum\vlinebotsp>0 %
      \setlength{\dimen0}{\vlinetopsp sp}%
      \setlength{\dimen2}{\vlinebotsp sp}%
      \edef\vlinetoppt{\the\dimen0}%
      \edef\vlinebotpt{\the\dimen2}%
      \begin{tikzpicture}[overlay,remember picture]
        \draw[color=black,line width=0.4pt]
          ([xshift=1mm,yshift=\vlinetoppt]current page.south)
          -- ([xshift=1mm,yshift=\vlinebotpt]current page.south);
      \end{tikzpicture}%
    \fi
  \fi
  \endgroup
}

% ══════════════════════════════════════════════════════════════════════════
% examheader v3 · 시안 A v3
%   헤더 값만 우리 시험지에 맞게 조정 : 17문항·100점 · 50분
% ══════════════════════════════════════════════════════════════════════════
\makeatletter
\renewcommand{\examheader}[2]{%
  \thispagestyle{empty}%
  \par\noindent
  % (1) 상단 hairline · 페이지 폭 전체
  {\color{black}\rule{\linewidth}{0.3pt}}\par
  \vspace{4pt}
  % (2) SMALL CAPS 시리즈 라벨 (좌) · 회차 (우)
  \noindent
  {\sffamily\scriptsize\color{black}\textls[280]{DEPTH \& INSIGHT}}%
  \hfill
  {\sffamily\scriptsize\color{black}\textls[280]{2026 \textperiodcentered\ 2 SEMESTER}}%
  \par\vspace{5pt}
  % (3) 대타이틀 (교재명 · 좌) + 회차 (우) · 산세리프 32pt bold
  \noindent
  {\fontsize{32pt}{34pt}\selectfont\sffamily\bfseries\color{black}%
    \examround\hfill\examvol}%
  \par\vspace{2pt}
  % (4) subtitle : 대단원명
  \noindent
  {\sffamily\normalsize\color{black}#1}%
  \par\vspace{2pt}
  % (4.5) 부제 아래 가로선
  {\color{black}\rule{\linewidth}{0.3pt}}\par
  \vspace{4pt}
  % (5) 3-column grid : 대상 · 문항·배점 · 시간
  \noindent
  \makebox[\linewidth][s]{%
    \begin{minipage}[t]{0.32\linewidth}%
      {\sffamily\scriptsize\color{black}\textls[200]{TARGET}}\par\vspace{2.5pt}
      {\sffamily\small\color{black}2학기 중간고사 대비}%
    \end{minipage}%
    \hfill
    {\color{black}\vrule width 0.3pt height 7.5mm}%
    \hfill
    \begin{minipage}[t]{0.30\linewidth}%
      \centering
      {\sffamily\scriptsize\color{black}\textls[200]{ITEMS \textperiodcentered\ POINTS}}\par\vspace{2.5pt}
      {\sffamily\small\color{black}17문항 \textperiodcentered\ 100점}%
    \end{minipage}%
    \hfill
    {\color{black}\vrule width 0.3pt height 7.5mm}%
    \hfill
    \begin{minipage}[t]{0.28\linewidth}%
      \raggedleft
      {\sffamily\scriptsize\color{black}\textls[200]{TIME}}\par\vspace{2.5pt}
      {\sffamily\small\color{black}50분}%
    \end{minipage}%
  }%
  \par\vspace{5pt}
  % (6) 하단 hairline
  \noindent{\color{black}\rule{\linewidth}{0.3pt}}\par
  \vspace{3pt}
  % (7) 구성 (좌) + 이름 [ 필기 공백 ] (우)
  \noindent
  {\sffamily\footnotesize\color{black}%
    선택형 12문 \textperiodcentered\ 서술형 5문%
    \hfill
    이름\ [\hspace*{55mm}]}%
  \par\vspace{3pt}
  % (8) 종결 가로선 (세로 분할선 시작점)
  \noindent{\color{black}\rule{\linewidth}{0.4pt}}\dmvlinetop\par
  \addvspace{2mm}
  \thispagestyle{examfooter}%
}
\makeatother

% ── 학원 로고·통상 페이지 헤더·푸터 ──
\makeatletter
\fancyfoot[R]{\raisebox{-0.8mm}{\includegraphics[height=4mm]{학원로고 (깊이와통찰).jpg}}}
\renewcommand{\headrule}{\hbox to\headwidth{\color{black}\leaders\hrule height \headrulewidth\hfill}\dmvlinetop}
\renewcommand{\footrule}{\dmvlinebot\hbox to\headwidth{\color{black}\leaders\hrule height \footrulewidth\hfill}\vskip 4pt}
\fancypagestyle{examfooter}{%
  \fancyhf{}%
  \fancyfoot[L]{}%
  \fancyfoot[C]{\sffamily\bfseries\color{black}-\,\thepage\,-}%
  \fancyfoot[R]{\raisebox{-0.8mm}{\includegraphics[height=4mm]{학원로고 (깊이와통찰).jpg}}}%
  \renewcommand{\headrulewidth}{0pt}%
  \renewcommand{\footrulewidth}{0.4pt}%
  \renewcommand{\footrule}{\dmvlinebot\hbox to\headwidth{\color{black}\leaders\hrule height \footrulewidth\hfill}\vskip 4pt}%
}

% ── 문항 스타일 (완전 검정 통일) ──
\renewenvironment{problem}[2]{%
  \par\setcounter{probnum}{#1}%
  \noindent
  \begin{minipage}[t]{\linewidth}%
  \raggedright%
  \setlength{\parskip}{4pt plus 1pt}%
  {\sffamily\bfseries\large\color{black}#1.}\hspace{4pt}\ignorespaces
}{%
  \end{minipage}\par%
}
\renewcommand{\pointbadge}[1]{}
\newcommand{\pts}[1]{%
  \nobreak\hspace*{\fill}%
  \mbox{\sffamily\small\color{black}[\textbf{#1점}]}%
  \par%
}
\makeatother

% ── 학평 배지 로컬 재정의 (크기 축소 · 한글 폴백 경고 해소) ──
\renewcommand{\probsource}[1]{{\scriptsize\sffamily\color{black}#1}}

\begin{document}

\examheader{도형의 방정식 \textperiodcentered\ 집합과 명제}{총 17문 \textperiodcentered\ 100점}

% ══════════════════════════════════════════════════════════════════════════
% 페이지 1 (선택형 Q1~Q4) · examheader 동반 페이지
% 옵션 A 세로 컬럼 (\pagecolumnpair): 좌 {1,2} · 우 {3,4}
% 🔵 2026-08-05 세션 115 — 선택형 열둘을 맹검 실측값 순으로 다시 늘어놓았다
% ══════════════════════════════════════════════════════════════════════════
\begin{pagepair}
\pagecolumnpair{%
% 🔵 자리 옮김 (2026-08-05 세션 115 · 맹검 실측 2.33) — 옛 2번 → 새 1번
%    🔴 아래 주석에 적힌 「N번」 은 옛 번호다. 대조표를 봐라.
% 원본: 와부고 2022 2학기 중간 1번 (대칭차의 원소 개수)
%   변형 — 숫자 교체 · 묻는 방향을 뒤집어 n(A) 를 되짚게 함 · 두 차집합의 크기 비를 얹음
%   답 ④ 29  (n(A∪B)=37 · 두 차집합 합 24 · n(B-A)=8 · n(A-B)=16 · 16+13=29)
\begin{problem}{1}{s}
전체집합 $U$의 두 부분집합 $A$, $B$가
$$n(U)=45,\qquad n(A^{c}\cap B^{c})=8,\qquad n(A\cap B)=13$$
을 만족시킨다. $n(A-B)=2\,n(B-A)$일 때, $n(A)$의 값은? \pts{4.5}
\begin{choices}
\choice{$16$}
\choice{$21$}
\choice{$24$}
\choice{$29$}
\choice{$37$}
\end{choices}
\end{problem}

\vfill

% 🔵 자리 옮김 (2026-08-05 세션 115 · 맹검 실측 2.90) — 옛 1번 → 새 2번
%    🔴 아래 주석에 적힌 「N번」 은 옛 번호다. 대조표를 봐라.
% 원본: 와부고 2021 1학기 중간 13번 (세 수선이 한 점에서 만나는 점의 좌표)
%   변형 — 좌표 교체(좌우 대칭을 깬다) · 세 수선을 두 수선으로 · 좌표쌍 대신 y좌표만
%   답 ④ 1  (AC 기울기 -2 → 수선 기울기 1/2 · AB 기울기 3 → 수선 기울기 -1/3 · 교점 (0,1))
\begin{problem}{2}{s}
세 점 $\mathrm{A}(0,\,6)$, $\mathrm{B}(-2,\,0)$, $\mathrm{C}(3,\,0)$을 꼭짓점으로 하는 삼각형 $\mathrm{ABC}$가 있다. 점 $\mathrm{B}$에서 변 $\mathrm{AC}$에 내린 수선과 점 $\mathrm{C}$에서 변 $\mathrm{AB}$에 내린 수선이 만나는 점을 $\mathrm{H}$라 하자. 점 $\mathrm{H}$의 $y$좌표는? \pts{4.6}
\begin{choices}
\choice{$-6$}
\choice{$-1$}
\choice{$0$}
\choice{$1$}
\choice{$6$}
\end{choices}
\end{problem}
}{%
% 🔵 자리 옮김 (2026-08-05 세션 115 · 맹검 실측 3.40) — 옛 5번 → 새 3번
%    🔴 아래 주석에 적힌 「N번」 은 옛 번호다. 대조표를 봐라.
% 🔴🔴 2026-08-05 세션 115 재설계 2차 (마스터 「이대로는 안 돼. 난이도가 쉬워」)
%    맹검 실측 3.83 (1회 5번은 4.37). 걸음은 다섯인데 **어느 걸음에서도 고를 것이 없었다.**
%    필요한 값이 기울기 ±3/4 하나뿐이고 그 하나가 「중심에서 접선까지의 거리 = 반지름」 한 도구에서 나왔다.
% 🔴 원본을 옮겼다 — 2023 1학기 기말 4번 → **2021 1학기 중간 15번**
%    (원 위의 점에서의 접선이 다른 원과 두 점에서 만날 조건)
% 변형 — 숫자(반지름 2→3 · 다른 원의 중심 (6,0)→(8,0)) · **묻는 방향을 뒤집었다** (만난다 → 만나지 않는다)
% 🔴 뒤집기가 왜 먹히나 — 원본대로 「만난다」면 답 1/3<a<1 이 저절로 -3≤a≤3 안에 들어앉아
%    「P 가 원 위에 있다」가 답에 흔적을 안 남긴다. 뒤집으면 답이 양쪽으로 뻗어서
%    🔴 **학생이 -3≤a≤3 을 스스로 꺼내 걸어야 닫힌다. 문제는 그 말을 안 한다.**
% 🔴 값이 하나에서 셋으로 늘고 셋이 다른 도구에서 나온다 —
%    접선 ax+by=9 (원 위의 점에서의 접선) · |8a-9|/3>1 (거리와 반지름) · -3≤a≤3 (점이 원 위에 있다는 사실)
%    셋째가 앞의 둘 어디에서도 안 나온다. 걸음 수는 다섯에서 넷으로 **줄었다.** 손을 안 늘렸다.
% 🟢 종전 5번의 「넓이」가 13번과 결이 닿아 있었는데 새 안은 넓이를 아예 버린다.
% 답 -3 ≤ a < 3/4 또는 3/2 < a ≤ 3   (경계 둘에서 거리가 꼭 1 이다)
\begin{problem}{3}{s}
원점을 중심으로 하고 반지름의 길이가 $3$인 원 $C$ 위의 점 $\mathrm{P}(a,\,b)$에서 원 $C$에 그은 접선을 $l$이라 하자. 직선 $l$이 점 $(8,\,0)$을 중심으로 하고 반지름의 길이가 $1$인 원 $D$와 만나지 않도록 하는 $a$의 값의 범위는? \pts{4.7}
% 🔴🔴 2026-08-05 세션 115 검수 반영 — **선지가 관문을 대신 넘겨주고 있었다.**
%    종전 선지는 ①②③ 이 모두 「-3≤a」 로 시작했다. 이 문항의 관문은 바로 그 울타리를
%    학생이 스스로 꺼내는 것인데(위 주석 참조), **문제가 안 하는 말을 선지가 세 번 했다.**
%    🔴 「발문에 방법 지시 금지」(SIMULATOR-와부-디자인-v1.md:231)가 발문 문장만 보고 선지는 안 본다.
%       발문이 안 흘린 것을 선지가 흘리면 같은 결함이다.
%    고친 법 — **오답 값만 다시 지었다.** 울타리가 든 것을 셋에서 둘로 줄이고 안 든 것을 셋으로 늘렸다.
%    이제 「울타리가 있는 것만 고르면 된다」는 지름길이 죽는다. ④ 도 울타리는 있으나 경계가 다르다.
%    🔴 수학·정답값·정답 자리(①)는 그대로다. 급 판정이 낡지 않는다.
%    오답이 나오는 자리 —
%      ② 울타리를 아예 안 걸었다
%      ③ 부등호를 뒤집어 「만나는」 조건으로 풀었다
%      ④ 중심에서 접선까지의 거리를 원 D 의 반지름 1 이 아니라 원 C 의 3 과 견줬다
%         (|8a-9|/3>3 → a<0 또는 a>9/4)
%      ⑤ 접선을 ax+by=3 으로 세웠다 (반지름의 제곱 자리에 반지름을 넣었다) · 울타리도 안 걸었다
%         (|8a-3|/3>1 → a<0 또는 a>3/4)
\begin{choicesv}%
{$-3\le a<\dfrac34$ 또는 $\dfrac32<a\le 3$}%
{$a<\dfrac34$ 또는 $a>\dfrac32$}%
{$\dfrac34<a<\dfrac32$}%
{$-3\le a<0$ 또는 $\dfrac94<a\le 3$}%
{$a<0$ 또는 $a>\dfrac34$}%
\end{choicesv}
\end{problem}

\vfill

% 🔵 자리 옮김 (2026-08-05 세션 115 · 맹검 실측 3.30) — 옛 11번 → 새 4번
%    🔴 아래 주석에 적힌 「N번」 은 옛 번호다. 대조표를 봐라.
% 원본: 와부고 2024 1학기 기말 논술형 3(18번) + 같은 회차 12번(보기 틀)
%   변형 — 대칭이동시킬 직선의 기울기를 m 으로 살려 둠 · 서술형을 보기형으로
%   🔴 최상단 조건 — ㄷ 의 후보를 학생이 만든다. 두 근 중 일치하는 쪽을 스스로 걸러야 한다
%   답 ③ ㄱ, ㄴ  (원점의 대칭점 O'(-12/5, 6/5) · ㄷ 은 m=2 하나뿐이라 합이 2)
% 🔴🔴🔴 2026-08-05 세션 115 검수 반영 — **7번과 사실상 같은 문항이었다.**
%    검수자 둘이 따로따로 잡았다.
%      층    7번(종전)                        11번(종전)
%      ㄱ    특정 값(k=0)을 넣어 확인 → 참     특정 값(m=0)을 넣어 확인 → 참
%      ㄴ    개수를 묻는다 → 참                불변을 묻는다 → 참
%      ㄷ    **모든 값의 합**이 어긋난다 → 거짓  **모든 값의 합**이 어긋난다 → 거짓
%      정답  ③ ㄱ,ㄴ                           ③ ㄱ,ㄴ
%    🔴 선지 다섯 중 넷이 글자까지 같았다. 「7번을 푼 학생은 11번을 안 풀고 ③ 을 찍는다」
%    🔴 내가 「7번은 세 직선, 11번은 대칭이동」이라고 **도구만 보고 넘긴 자리**다. 껍데기를 안 봤다.
%
% 🔴 보기형을 버렸다. 그것이 7번과 갈리는 가장 확실한 길이다.
%    보기형이 끌어당기는 짜임(확인 → 일반화 → 값의 합)이 하나라, 형식을 남기면 또 닮는다.
%    1회 10번까지 세면 이 회차가 셋째 보기형이 된다.
%    🔴 청사진의 「보기형」 기재는 형식을 적은 것이라 따르지 않았다.
%
% 🔴 원본을 갈았다 — 2024 1학기 기말 18번+12번 → **2019 1학기 기말 17번** (서술형 2 · 7.0점)
%    「한 변이 √10 인 정사각형 ABCD · A 는 y축 위 · B(1,0) · 직선 CD 의 방정식」
%    🔴 2022·2023·2024 1학기 중간은 셋 다 다항식~이차함수라 도형의 방정식이 한 문항도 없다
%       (제안자가 2024 1학기 중간을 열어 확인했다).
% 변형 셋 — (가) 그림과 한 변의 길이를 빼고 A(0,p) 로 문자를 살렸다
%           (나) 묻는 방향을 뒤집어 D 의 자리로 되짚게 했다
%           (다) 직선의 방정식을 y절편으로 좁혔다
%    🔴 (다)의 까닭 — 원본대로 「직선 CD 의 방정식은?」 으로 두면 **선지에 기울기 -3 이 찍혀**
%       셋째 관문(CD 가 AB 와 나란하다)이 반쯤 넘어간다. 이번 회차의 5·8번 결함과 같은 꼴이다.
% 🔴 관문 셋 — 문자 세우기 · D 의 후보 둘 만들기 · CD 가 AB 와 나란함.
%    갈래가 둘이라는 말이 발문에 없다. 뒤쪽 후보는 어떤 p 에서도 x+y=7 위에 못 온다.
% 🟢 원 병에 안 보탠다 — 「중심에서 잰 거리 vs 반지름」을 한 번도 안 쓴다.
% 🟢 3번(도형의 이동)과의 겹침이 통째로 없어진다 — 새 11번에는 이동이 아예 없다.
% 🔴 그림 없음 — 그리면 갈래가 하나로 굳고 CD 가 AB 와 나란한 것도 눈에 바로 보인다.
% 답 13  (A(0,3) · D(3,4) · C(4,1) · 직선 CD: 3x+y-13=0 · 변의 길이 제곱 10 · 내적 0)
% 🔴🔴 첫 재설계안(정사각형 · A 는 y축 위)은 폐기했다 — **16번과 원본이 부딪혔다.**
%    둘 다 2019 1학기 기말 17번을 골랐다. 같은 시각에 도는 두 사람이 서로를 못 본 것이다.
%    🔴 「이미 쓴 번호」 표는 **과거만 담는다.** 병렬로 도는 것들끼리는 그 표에 없다.
%       세션 114 에도 같은 사고(7번·12번이 같은 원본)가 났다.
%    16번이 서술형이고 두 원을 없애 원 쏠림을 푸는 값이 커서 16번을 남기고 11번을 다시 뽑았다.
%
% 🔴 원본 — 와부고 **2016 1학기 기말 8번** (선택형 4.2점 · 처음 여는 회차)
%    「직선 (k-3)x+(k-1)y+2=0 이 k 에 관계없이 항상 지나는 점과 직선 x+2y-4=0 사이의 거리」
%    🟢 「정점을 지나는 직선」은 와부고 단골이다 — 2017 1학기 기말 9번이 같은 도구를 보기형으로 낸다.
% 변형 — 상대 직선을 없애고 점 A(3,5) 를 놓은 뒤 **직선 l 을 움직인다.**
%    정점이 답에서 **디딤돌로 내려간다.** 원본은 정점을 구하면 끝인데 여기는 거기서 시작한다.
% 🔴 두 값이 서로 다른 도구에서 나온다 —
%    정점 (1,-1) 은 **k 에 대한 항등식**에서, k=4 는 **수직 조건**에서.
% 🔴 발문이 안 알려 주는 관문 — 「가장 멀다」를 「l 이 AP 에 수직」으로 옮기는 일.
% 🔴 선지가 관문을 안 흘린다 — 관문 셋이 다 「모양」인데 선지는 수 하나다.
%    제안자가 「직선의 방정식은?」·「그 거리는?」 두 워딩을 시험해 보고 **둘 다 관문을 흘려서 버렸다**
%    (기울기가 다섯 번 찍히거나, 2√10 이 보이는 순간 AP 를 재면 된다는 것이 드러난다).
%    정답 4 는 발문에 안 나오는 수다.
% 🟢 보기형 아님 · 원 안 씀 · 넓이 안 씀 · 「최댓값」·「모두」 안 씀
% 답 4  (정점 (1,-1) · |AP|=2√10 · k=4 일 때 l: x+3y+2=0 · 거리가 꼭 2√10)
\begin{problem}{4}{s}
직선 $l:(k-3)x+(k-1)y+2=0$은 실수 $k$의 값에 관계없이 항상 일정한 점을 지난다. 점 $\mathrm{A}(3,\,5)$와 직선 $l$ 사이의 거리가 가장 멀 때, 실수 $k$의 값은? \pts{4.8}
\begin{choices}
\choice{$\dfrac{3}{2}$}
\choice{$\dfrac{5}{2}$}
\choice{$3$}
\choice{$4$}
\choice{$5$}
\end{choices}
\end{problem}
}
\end{pagepair}

% ══════════════════════════════════════════════════════════════════════════
% 페이지 2 (선택형 Q5~Q8) · 좌 {5,6} · 우 {7,8}
% ══════════════════════════════════════════════════════════════════════════
\begin{pagepair}
\pagecolumnpair{%
% 🔵 자리 옮김 (2026-08-05 세션 115 · 맹검 실측 3.40) — 옛 8번 → 새 5번
%    🔴 아래 주석에 적힌 「N번」 은 옛 번호다. 대조표를 봐라.
% 원본: 와부고 2025 2학기 기말 4번 (「모든」·「어떤」이 든 명제의 참·거짓) — 회차·번호 그대로
%   🔴🔴 2026-08-05 세션 115 재설계 — 맹검 실측 2.93 (배점 5.1 인데 시험지에서 아래에서 둘째였다).
%      판정자 셋이 3.0 · 2.9 · 3.0 으로 흔들림 없이 같은 값을 줬다.
%      까닭: 원소 다섯을 명제 다섯에 넣어 보는 손일이라 학생이 스스로 만들 것이 없었다.
%   변형 — **묻는 방향을 뒤집었다.** 원본은 U 를 고정하고 명제를 골랐는데, 여기는 명제를 고정하고 U 를 찾는다.
%      같은 회차 서술형 2(14번)가 「명제의 역이 참이 되도록 하는 자연수의 최솟값」이라 이 방향은 그 회차 안에 있다.
%      한정어 「모든」·「어떤」과 원소 넷은 원본 그대로.
%   🔴 두 조건이 서로 다른 도구에서 나오고 방향도 반대다 —
%      (가)는 이차부등식 → U 가 **피해야 할** 정수 (0 과 1)
%      (나)는 이차방정식의 두 근 → U 가 **덮어야 할** 정수 (3 또는 -1)
%   🔴 문자를 a 에서 b 로 갈았다 — 12번이 실수 a, 17번이 a 를 이미 쓴다. 셋이 되면 눈에 걸린다.
%   답 1  (b = -4, 2, 3 셋 · 합 1)
% 🔴🔴 2026-08-05 세션 115 재설계 2차 (마스터 「이대로는 안 돼. 난이도가 쉬워」)
%    1차 재설계로 2.93 → 3.80 까지 올렸으나 1회 8번(4.40)에 아직 0.60 모자랐다.
%    까닭: 네 칸 창이 연이은 정수 넷이라 (나)가 후보 여덟을 **대신 만들어 줬다.**
%          학생이 경계를 스스로 잡는 자리가 없었다.
% 변형 — 뼈대는 반만 살렸다. 한정 명제는 그대로 두고 **문자를 U 밖에서 명제 안으로 옮겼다.**
%    그러면 답이 유한 목록이 아니라 **수직선 위의 구간**이 되고, 학생이 만들 것이 후보가 아니라 **경계 둘**이 된다.
% 🔴 두 값이 정반대 방향에서 나온다 —
%    상한 4 : 「모든」이라 다섯 조건의 **교집합** → 가장 **센** 것을 고른다 (x=2)
%    하한 3 : 「어떤」이라 다섯 조건의 **합집합** → 가장 **헐거운** 것을 고른다 (x=3)
%    한쪽 손놀림을 다른 쪽에 그대로 쓰면 틀린다.
% 🔴 이기는 원소가 U 의 양 끝이 아니다. (나)의 갈래 둘 가운데 b<-5 는 (가)의 b>-5 와 맞물려 죽는다.
%    갈래가 둘이라는 것도, 하나가 죽는다는 것도 발문에 없다.
% 🟢 4번·12번과 갈린다 — 4번은 자연수 하나로 끝나는 세는 문항, 12번은 값의 합.
%    8번은 구간으로 끝나는 재는 문항이다. 종전에 12번과 발문 꼴이 닮았다던 지적이 함께 풀린다.
% 답 3<b<4  (b 를 0.001 씩 훑어 만족 구간이 하나뿐임을 확인했다)
\begin{problem}{5}{s}
실수 $b$에 대하여 전체집합 $U=\{-1,\ 0,\ 1,\ 2,\ 3\}$이 있다. $x\in U$일 때, 두 명제
\begin{conditions}
(가) 모든 $x$에 대하여 $x^{2}-bx+4>0$이다.\\
(나) 어떤 $x$에 대하여 $x^{2}-2bx+9<0$이다.
\end{conditions}
가 모두 참이 되도록 하는 실수 $b$의 값의 범위는? \pts{4.9}
% 🔴🔴 2026-08-05 세션 115 검수 반영 — **선지가 관문을 대신 넘겨주고 있었다.**
%    종전 선지는 넷이 「3<b<」 로 시작해 **아래쪽 경계 3 이 공짜**였다.
%    이 문항의 관문은 경계 둘을 각각 다른 방향으로 잡는 것인데 그 절반을 선지가 줬다.
%    고친 법 — **오답 값만 다시 지었다.** 아래쪽 경계가 서로 다른 값이 되도록 골라
%    학생이 하한과 상한을 **둘 다 스스로 정해야** 고를 수 있게 했다.
%    🔴 수학·정답값·정답 자리(②)는 그대로다. 급 판정이 낡지 않는다.
%    오답이 나오는 자리 —
%      ① (나)를 아예 안 썼다 → (가)만 남아 -5<b<4
%      ③ 둘 다 잘못 골랐다 — 「어떤」에서 x=2(하한 13/4) · 「모든」에서 x=3(상한 13/3)
%      ④ 「모든」에서 가장 센 것(x=2)이 아니라 x=3 을 골라 상한이 13/3
%      ⑤ (나)의 갈래 b<-5 가 (가)와 맞물려 죽는 것을 못 봤다
\begin{choicesv}%
{$-5<b<4$}%
{$3<b<4$}%
{$\dfrac{13}{4}<b<\dfrac{13}{3}$}%
{$3<b<\dfrac{13}{3}$}%
{$b<-5$ 또는 $3<b<4$}%
\end{choicesv}
\end{problem}

\vfill

% 🔵 자리 옮김 (2026-08-05 세션 115 · 맹검 실측 3.47) — 옛 3번 → 새 6번
%    🔴 아래 주석에 적힌 「N번」 은 옛 번호다. 대조표를 봐라.
% 🔴🔴 2026-08-05 세션 115 재설계 2차 (마스터 「이대로는 안 돼. 난이도가 쉬워」)
%    맹검 실측 2.57 — 열일곱 자리에서 낙차가 가장 컸다 (1회 3번은 3.30).
%    까닭: 부호를 바꾸고 문자를 맞바꾸고 거리 공식에 넣는 정해진 길이었다.
%          대칭이동을 두 번 시킨 것이 겹 같지만 **같은 손놀림을 두 번** 하는 것이라 생각이 한 번 더 안 갔다.
% 🔴 원본을 옮겼다 — 2024 1학기 기말 5번 → **2023 1학기 기말 2번**
%    (점을 y=x 와 x축에 대칭이동시켜 좌표를 묻는 문항)
%    종전 원본으로는 겹을 얹으려면 「직선을 직선에 대칭이동 + 문자 살리기」로 갈 수밖에 없는데
%    🔴 그 자리는 11번이 이미 가졌다.
% 변형 — 원본이 겉으로 물었던 두 대칭이동 가운데 **하나를 감췄다.**
%    y=x 대칭은 발문에 남기고 x축 대칭은 지웠다. 학생이 대칭점 A'(1,-4) 를 스스로 만들어야 한다.
%    최단 거리 문제라는 것도 발문이 말하지 않는다.
% 🔴 두 값이 서로 다른 도구에서 나온다 —
%    a=7 은 두 점 사이의 거리에서, 답인 x좌표 5 는 두 점을 지나는 직선의 절편에서.
%    🔴 버릴 후보가 진짜로 생긴다 — a=-5 도 같은 거리를 주고 단서 a>0 이 하나를 지운다.
% 답 5  (B(7,2) · A'(1,-4) · 직선 y=x-5 · x절편 5 · P 에서 실제 합 6√2)
% 🔴 그림 없음 — 대칭점을 어디에 찍는지가 관문이라 그림이 답을 흘린다.
\begin{problem}{6}{s}
좌표평면 위의 점 $\mathrm{D}(2,\,a)$를 직선 $y=x$에 대하여 대칭이동한 점을 $\mathrm{B}$라 하자. 점 $\mathrm{A}(1,\,4)$와 $x$축 위의 점 $\mathrm{P}$에 대하여 $\overline{\mathrm{AP}}+\overline{\mathrm{PB}}$의 최솟값이 $6\sqrt{2}$이다. 이때 $\overline{\mathrm{AP}}+\overline{\mathrm{PB}}$가 최소가 되는 점 $\mathrm{P}$의 $x$좌표는? \cond{$a>0$이다.} \pts{5.0}
\begin{choices}
\choice{$-5$}
\choice{$-3$}
\choice{$5$}
\choice{$7$}
\choice{$13$}
\end{choices}
\end{problem}
}{%
% 원본: 와부고 2022 1학기 기말 2번 (세 직선이 삼각형을 이루지 않는 k 의 합)
%   변형 — 숫자 교체(두 고정 직선의 교점을 (2,1) 로) · 발문을 보기형으로
%   🔴 갈래가 몇인지 발문이 알려 주지 않는다. ㄷ 이 값을 던져 후보를 학생이 다 만들게 한다
%   답 ③ ㄱ, ㄴ  (k=1/2, -1, 1 셋 · 합 1/2 이라 ㄷ 은 거짓)
\begin{problem}{7}{s}
세 직선
% 🔴 세 식을 한 줄에 두면 컬럼(73.6mm)을 22.6pt 넘긴다 (1차 빌드 실측). 두 줄로 나눈다.
$$l:2x+y-5=0,\qquad m:x-y-1=0,$$
$$n:x+ky-3=0$$
에 대하여 옳은 것만을 다음 〈보기〉에서 있는 대로 고른 것은? \cond{$k$는 실수이다.} \pts{5.0}
\vspace{2mm}

\begin{bogibox}
ㄱ. $k=0$이면 세 직선은 삼각형을 이룬다.\\[2pt]
ㄴ. 세 직선이 삼각형을 이루지 않도록 하는\\[1pt]
\hphantom{ㄴ. }\nob{실수 $k$는 세 개이다.}\\[2pt]
ㄷ. 세 직선이 삼각형을 이루지 않도록 하는\\[1pt]
\hphantom{ㄷ. }\nob{모든 실수 $k$의 값의 합은 $-\dfrac12$이다.}
\end{bogibox}
\begin{choices32}{ㄱ}{ㄷ}{ㄱ,ㄴ}{ㄱ,ㄷ}{ㄱ,ㄴ,ㄷ}\end{choices32}
\end{problem}

\vfill

% 🔵 자리 옮김 (2026-08-05 세션 115 · 맹검 실측 4.33) — 옛 6번 → 새 8번
%    🔴 아래 주석에 적힌 「N번」 은 옛 번호다. 대조표를 봐라.
% 원본: 와부고 2023 2학기 중간 14번 (배수 집합의 연산과 정수의 개수)
%   변형 — 숫자만 (45→63 · 420→588 · 500 이하→600 이하). 미끼(3의 배수이나 9의 배수 아님)를 살렸다
%   답 ② 13  ((63,n)=21 → n=21k, k는 3의 배수 아님 · k≤28 에서 19개 · 588의 약수 6개 제외)
\begin{problem}{8}{s}
자연수 전체의 집합을 $U$라 하고, 자연수 $n$에 대하여 $U$의 부분집합 $A_{n}$을
$$A_{n}=\{x\mid x\text{는 }n\text{의 배수}\}$$
라 하자. 두 조건
$$A_{63}\cap A_{n}=A_{3n},\qquad 588\in A_{n}^{\,c}\cup A_{9}$$
를 모두 만족시키는 $600$ 이하의 자연수 $n$의 개수는? \pts{5.1}
\begin{choices}
\choice{$6$}
\choice{$13$}
\choice{$19$}
\choice{$22$}
\choice{$28$}
\end{choices}
\end{problem}
}
\end{pagepair}

% ══════════════════════════════════════════════════════════════════════════
% 페이지 3 (선택형 Q9~Q12) · 좌 {9,10} · 우 {11,12}
% ══════════════════════════════════════════════════════════════════════════
\begin{pagepair}
\pagecolumnpair{%
% 🔴🔴 2026-08-05 세션 115 검수 반영 — 둘이 겹쳤다.
%    ① 재현-1회 2번(원 밖의 점에서 원 위의 점까지 거리의 최대·최소)과 같은 결이었다.
%       무게중심 겹은 1회 1번과도 닿았다.
%    ② 🔴 **이 회차에 원이 너무 많다.** 「중심에서 잰 거리 vs 반지름」이
%       5·9·12·15·16·17 여섯 문항을 관통해 100점 중 41.6점이었다.
%       1회 목록의 「두 원 관계를 줄인다」는 지켰는데(16번 하나) **원 자체는 늘었다.**
%       글자대로 지키고 뜻을 놓친 것이다.
%
% 🔴 원본을 갈았다 — 2022 1학기 기말 8번 → **2019 1학기 기말 13번** (선택형 4.9점)
%    「세 점으로 만든 삼각형에서 AC=AD 로 D 를 잡고, A 를 지나 DC 와 평행한 직선이
%      BC 연장선과 만나는 점 P · 넓이비」
%    🔴 **원이 한 개도 안 나온다.** 두 점 사이의 거리 · 평행한 직선 · 길이비 · 좌표로 잰 넓이뿐이고
%       이 결은 1회에도 2회에도 없다.
%    🔴 내가 준 원본 지도가 틀렸다 — 「2022·2023·2024 1학기 중간을 보라」고 했는데
%       셋 다 다항식~이차함수라 **도형이 0문**이다. 2020·2021 1학기 중간에 도형이 있었던 것은
%       그 두 해가 시험 일정이 밀려 한 회차가 수학(상)을 통째로 덮었기 때문이다 (제안자가 확인).
%
% 변형 셋 — (가) 방향을 뒤집었다. D 를 정해 주고 넓이를 묻던 것을 넓이를 주고 D 를 되짚게 했다
%           (나) 두 값을 요구한다 — D 의 x좌표는 넓이 방정식에서, P 의 x좌표는 평행선과 좌표에서
%           (다) P 가 어디서 만나는지 안 알려 준다 (원본의 「선분 BC 의 연장선」 → 「직선 BC」)
% 🔴 계산이 오히려 깨끗해졌다 — 원본은 P 가 (67/3, -23/3) 인데 새 안은 D(-5,3) · P(37,-13) 로 다 정수다.
% 🔴 「선분 AB 위의 점 D」가 답을 하나로 만든다 — 이 단서가 없으면 x=-11 도 넓이 48 을 준다.
%    (내가 이 단서를 안 읽고 「답이 둘」이라 잘못 짚었다. 제안자가 옳았다.)
% 🔴 선지가 관문을 안 흘린다 — 「합」으로 물어 D 와 P 를 갈라낼 수 없다.
%    P 의 x좌표만 물었다면 선지 37 을 직선 BC 에 대입해 관문을 건너뛸 수 있었다.
% 답 32  (삼각형 ABC 넓이 16 · 3배 48 · D(-5,3) · P(37,-13) · -5+37)
\begin{problem}{9}{s}
세 점 $\mathrm{A}(1,\,3)$, $\mathrm{B}(-7,\,3)$, $\mathrm{C}(4,\,-1)$을 꼭짓점으로 하는 삼각형 $\mathrm{ABC}$가 있다. 선분 $\mathrm{AB}$ 위의 점 $\mathrm{D}$에 대하여, 점 $\mathrm{A}$를 지나고 직선 $\mathrm{DC}$와 평행한 직선이 직선 $\mathrm{BC}$와 만나는 점을 $\mathrm{P}$라 하자. 삼각형 $\mathrm{ACP}$의 넓이가 삼각형 $\mathrm{ABC}$의 넓이의 $3$배일 때, 두 점 $\mathrm{D}$, $\mathrm{P}$의 $x$좌표의 합은? \pts{5.2}
\begin{choices}
\choice{$12$}
\choice{$18$}
\choice{$32$}
\choice{$36$}
\choice{$43$}
\end{choices}
\end{problem}

\vfill

% 🔵 자리 옮김 (2026-08-05 세션 115 · 맹검 실측 4.30) — 옛 4번 → 새 10번
%    🔴 아래 주석에 적힌 「N번」 은 옛 번호다. 대조표를 봐라.
% 🔴🔴 2026-08-05 세션 115 재설계 2차 (마스터 「이대로는 안 돼. 난이도가 쉬워」)
%    맹검 실측 3.00 (1회 4번은 3.50). 정의를 그대로 적용하는 판단 다섯 번이라
%    손도 생각도 한 겹이었다. 원본이 그 회차 1번, 곧 가장 가벼운 자리였다.
% 🔴 원본을 옮겼다 — 2024 2학기 중간 1번 → **2022 2학기 중간 11번**
% 변형 — **묻는 방향을 뒤집었다.** 원본은 X 의 개수를 묻는데, 여기는 개수를 주고 a 를 되짚는다.
%    🔴 문자 a 를 살려 둬서 A 를 첫 줄에 적을 수 없다. 원본은 A={1,2,4} 를 적고 시작한다.
% 🔴 관문 — 두 조건을 겹쳐야 모순이 서고 「X ⊂ A」가 나온다. 조건 하나만으로는 아무것도 안 정해진다.
%    (가) 는 X-A ⊂ B · (나) 는 X∩B ⊂ A. x ∈ X-A 를 잡으면 (가)로 x ∈ B, (나)로 x ∈ A 라 모순이다.
% 🔴 두 값이 서로 다른 도구에서 나온다 — n(A)=3 은 부분집합의 개수에서, a=4·9 는 약수의 개수에서.
% 답 13  (X 의 개수 2^n(A) = 8 → n(A)=3 → a 는 소수의 제곱 → 4 와 9)
\begin{problem}{10}{s}
전체집합 $U=\{x\mid x\text{는 }10\text{ 이하의 자연수}\}$의 세 부분집합
$$A=\{x\mid x\text{는 }a\text{의 약수}\},$$
$$B=\{x\mid x\text{는 }10\text{ 이하의 소수}\}$$
와 $X$가 다음 조건을 모두 만족시킨다.
\begin{conditions}
(가) $(X-A)^{c}\cup B=U$\\
(나) $A^{c}\cap(X\cap B)=\varnothing$
\end{conditions}
이러한 집합 $X$의 개수가 $8$일 때, 모든 $a$의 값의 합은? \cond{$a$는 $10$ 이하의 자연수이다.} \pts{5.3}
\begin{choices}
\choice{$4$}
\choice{$8$}
\choice{$13$}
\choice{$18$}
\choice{$24$}
\end{choices}
\end{problem}
}{%
% 🔵 자리 옮김 (2026-08-05 세션 115 · 맹검 실측 4.63) — 옛 10번 → 새 11번
%    🔴 아래 주석에 적힌 「N번」 은 옛 번호다. 대조표를 봐라.
% 원본: 와부고 2023 2학기 중간 4번 (산술평균과 기하평균 · 최솟값) — 회차·번호 그대로
%   🔴🔴 2026-08-05 세션 115 재설계 — 맹검 실측 3.40. 열일곱 자리에서 가장 크게 내려앉았다 (1회 10번은 4.97).
%      이 자리는 청사진이 선택형 국소 정점(10·11·12)으로 잡은 곳인데 골짜기였다.
%      까닭: 종전 안은 분모가 x-1 이라 「x-1 로 묶어라」가 눈에 바로 보였고,
%            k 와 a 가 등호 조건 **한 줄에서 함께** 나왔다.
%   변형 — 원본 분자의 상수 1 을 문자 k 로 살렸다. 그것 하나다.
%   🔴 여기가 이 안의 값이다 — t = x²+1 이라 **t ≥ 1 이라는 울타리**가 있다.
%      k ≥ 1 이면 등호 자리 t=√k 가 울타리 안이라 산술기하가 통한다  → g(k) = 2√k − 1
%      k < 1 이면 등호 자리가 울타리 **밖**이라 산술기하가 못 쓴다     → g(k) = k  (t=1 경계에서 최소)
%      두 값이 **서로 다른 도구**에서 나온다. 갈래가 있다는 말도, 몇인지도 발문에 없다.
%   🔴 「모든」이라는 낱말을 뺐다 — 8번이 「모든」·「어떤」 한정 명제 자리다.
%   답 9/2  (p=4 · q=1/2)
%   🔴 오답 ⑤ 73/16 이 핵심 함정이다 — 두 번 다 산술기하로 밀면 q=9/16 이 나온다. 정답과 1/16 차이다.
\begin{problem}{11}{s}
실수 $k$에 대하여 함수
$$f(x)=x^{2}+\frac{k}{x^{2}+1}$$
의 최솟값을 $g(k)$라 하자. $g(k)=3$을 만족시키는 $k$의 값을 $p$, $g(k)=\dfrac{1}{2}$을 만족시키는 $k$의 값을 $q$라 할 때, $p+q$의 값은? \pts{5.4}
\begin{choices32}{$\dfrac{37}{16}$}{$\dfrac{7}{2}$}{$4$}{$\dfrac{9}{2}$}{$\dfrac{73}{16}$}\end{choices32}
\end{problem}

\vfill

% 원본: 와부고 2024 1학기 기말 11번 (교점의 총개수를 조건으로 하는 값 되짚기)
%   변형 — 직선 셋 중 하나를 원으로 바꾸고 움직이는 것을 원의 중심에 심음
%   🔴 청사진 role 의 「곡선」은 뺐다 — 곡선·원·직선의 교점 개수는 재현-1회 17번의 수학이다
%   답 ⑤ 27/2  (접함 a=1/2, 11/2 · R(15/4,1) 을 지나는 a=15/4±√3 · 합 6+15/2)
\begin{problem}{12}{s}
실수 $a$에 대하여 원 $C_{a}:(x-a)^{2}+y^{2}=4$와 두 직선
$$m:4x-3y-12=0,\qquad n:y=1$$
이 있다. 원 $C_{a}$가 직선 $m$ 또는 직선 $n$과 만나는 서로 다른 점의 개수를 $f(a)$라 하자. $f(a)=3$을 만족시키는 모든 실수 $a$의 값의 합은?
% 🔴 \cond 는 \mbox 라 줄이 안 끊긴다. 이 단서는 길어서 컬럼을 125.9pt 넘겼다 (1차 빌드 실측).
%    맨손으로 적어 줄바꿈을 연다. 짧은 단서(7·10·15번)는 \cond 를 그대로 쓴다.
(단, 원 $C_{a}$가 두 직선 $m$, $n$과 같은 점에서 만나는 경우 그 점은 $2$개가 아닌 $1$개로 센다.) \pts{5.5}
\begin{choices32}{$6$}{$\dfrac{15}{2}$}{$8$}{$13$}{$\dfrac{27}{2}$}\end{choices32}
\end{problem}
}
\end{pagepair}

% ══════════════════════════════════════════════════════════════════════════
% 페이지 4 (서술형 Q13 · Q14) — 좌 {13} · 우 {14 (1)(2)}
% ══════════════════════════════════════════════════════════════════════════
\begin{pagepair}
\probpairfillpage{%
% 원본: 와부고 2023 1학기 기말 8번 (넓이를 점과 직선 사이의 거리로 구하는 과정 · 빈칸형)
%   보조 원본: 2022 1학기 기말 6번 (밑변을 직선 위에 두고 꼭짓점을 바깥에)
%   변형 — 빈칸형을 서술형으로 · 삼각형 하나를 사각형을 대각선으로 자른 삼각형 둘로
%   답 36  (AC=4√5 · 직선 AC: x-2y+3=0 · 두 높이 11√5/5 와 7√5/5 · 22+14)
%   🔴 학생이 스스로 만들 것 — 두 삼각형의 밑변이 같은 선분이라는 것,
%      그리고 B 와 D 가 직선 AC 의 반대쪽에 있으므로 두 넓이를 더한다는 것
%   🔴 그림 없음 — 그림이 있으면 「반대쪽에 있다」가 눈에 바로 보여 학생이 확인할 것이 사라진다
%   🔴🔴 2026-08-05 세션 115 재설계 — 맹검 실측 2.70. 서술형 첫 자리인데 선택형 2번(2.20)보다
%      조금 무거울 뿐이었다. 배점은 6.0 이다.
%      까닭 셋 — ① 「두 거리를 각각 구하라」가 대각선으로 자른다는 착상을 미리 말해 줬다
%                ② 🔴 더 큰 것 — 두 거리가 **같은 공식에 숫자만 바꿔 넣은 것**이라 손만 한 번 더 갔다
%                ③ 좌표 넷을 다 주고 넓이를 물어 되짚을 자리가 없었다
%
%   🔴 신발끈을 어떻게 막았나 (마스터 확정 2026-08-04 「신발끈은 쓰지마」)
%      종전에는 「구할 값을 늘리는」 길로 막았는데 그것이 길을 깔아 줬다.
%      🔴 이번에는 **꼭짓점 하나를 좌표로 주지 않는다.** 신발끈에 넣을 숫자가 아예 없어진다.
%      발문에 방법을 지시하는 말이 한 마디도 없다 (SIMULATOR-와부-디자인-v1.md:231 준수).
%
%   원본은 그대로다 — 주 원본 2023 1학기 기말 8번 · 보조 2022 1학기 기말 6번. 회차 분포 변화 없음.
%   변형 — 넓이를 **조건으로 주고** 직선을 되짚게 했다. 삼각형의 한 꼭짓점이 직선 위를 움직인다.
%   🔴 관문 — ℓ 이 직선 AB 와 나란하다는 것을 발문이 말하지 않는다.
%      그래서 「C 가 어디에 있든 넓이가 같다」를 학생이 스스로 세워야 한다.
%      🔴 ℓ 을 y = x/2 + k 가 아니라 x-2y+k=0 으로 쓴 까닭이 이것이다 —
%         직선 AB 의 방정식을 세운 뒤에야 평행이 드러난다. 관문이 발문 표면에 안 뜬다.
%   🔴 두 도구 — 밑변은 두 점 사이의 거리, 높이는 두 평행선 사이의 거리. 서로 다른 곳에서 나온다.
%   🔴 그림 없음 — 그림이 있으면 ℓ 이 AB 와 나란한 것이 눈에 바로 보인다.
%   답 k = -5, 15  (직선 AB : x-2y+5=0 · 밑변 2√5 · 높이 |k-5|/√5 · 넓이 |k-5| = 10)
%   🔴 단서를 안 달았다 — k=5 면 ℓ 이 AB 와 겹쳐 넓이가 0 인데, 넓이 10 이라는 조건이 이미 걸러 낸다.
\begin{problem}{13}{s}
좌표평면 위의 두 점 $\mathrm{A}(-1,\,2)$, $\mathrm{B}(3,\,4)$와 직선
$$\ell:x-2y+k=0$$
이 있다. 직선 $\ell$ 위의 점 $\mathrm{C}$에 대하여 삼각형 $\mathrm{ABC}$의 넓이가 $10$일 때, 상수 $k$의 값을 모두 구하시오. (각 과정을 논술할 것) \pts{6.0}
\end{problem}
\vfill\null
}{%
% 🔴🔴 2026-08-05 세션 115 재설계 — 맹검 실측 2.97. **배점 대비 가장 크게 어긋난 자리였다.**
%    8.0점인데 2.97 이라, 4.6점짜리 선택형 2번(2.20)보다 조금 무거울 뿐이었다.
%    까닭: 교과서 증명을 그대로 옮겨 쓰는 일이라 학생이 만들 것이
%          「(1)을 m 에 한 번, n 에 한 번 두 번 쓴다」 하나뿐이었다.
%    🔴 판정자 셋이 따로따로 「이보다 얕은 서술을 견줄 앵커가 사다리에 없다」고 신고했다.
%
% 🔴 원본을 갈았다 — 2022 2학기 중간 18번(√3 무리수) → **2023 2학기 중간 17번**
%    「귀류법을 이용하여 다음 명제가 참임을 증명하시오.
%      자연수 n 에 대하여 (n²+1)(n²-1) 이 5의 배수가 아니면 n 은 5의 배수이다.」 [6.0점]
%    까닭: 와부고가 √3 무리수 증명을 세 해에 걸쳐 되풀이하면서 **보조 명제를 아예 얹어 준다.**
%          그 뼈대는 아무리 잘 옮겨도 학생이 만들 것이 안 는다.
%    🔴 회차 분포가 바뀐다 — 2023 2학기 중간 3 → 4 · 2022 2학기 중간 2 → 1.
%       여덟 회차에서 뽑는 것과 한 회차 최대 넷은 그대로다 (도형 쪽 2024 1기말도 4문이다).
%
% 변형 셋
%   (가) 곱을 풀어서 준다 — (n²+1)(n²-1) → n⁴-1. 같은 식이고 적는 꼴만 다르다.
%        🔴 그런데 곱으로 가르지 않으면 나머지 논의를 붙일 자리가 없어져 **관문이 하나 선다.**
%   (나) 앞 소문항을 「역」으로 — 학생이 역을 직접 쓰고 참·거짓을 판정한다.
%        🔴 역도 참이다 (n 이 5의 배수면 n⁴-1 을 5로 나눈 나머지가 언제나 4다).
%           「역은 대개 거짓」이라는 손버릇이 여기서 걸린다.
%        (2) 발문이 (1)을 쓰라고 지목하지 않는다 — 재료만 넘기고 방법은 안 넘긴다.
%   (다) 배점 4.0 : 4.0 → **3.0 : 5.0** (앞은 가볍게 · 합 8.0 불변)
%
% 🔴 학생이 스스로 만들 것 — n⁴-1 을 곱으로 가르고, 네 갈래마다 세 인수 중 어느 것이
%    5의 배수인지 스스로 고르는 일. 발문은 인수분해하라고도, 갈래가 몇이라고도 말하지 않는다.
% 🔴 발문에 방법 지시 없음 — 「귀류법을 이용하여 참임을 증명하시오」는 원본 문장 그대로다.
% 🔴🔴🔴 2026-08-05 세션 115 — 증명형을 통째로 뺐다 (마스터 확정)
%    > 「증명형은 출제하지 않는걸로 하자. 앵커를 만들기도 힘들거고 판정하기도 힘들고
%    >   답지 작성도 힘들어. 여러모로 일이 굴러가는 걸 해치니까 그냥 빼자. 14번을 바꿔.」
%    단일 출처: bank/마스터-지적/2026-08-05-증명형-출제-금지.md
%    🔴 세 곳이 한꺼번에 걸렸다 —
%       ① 판정자 열둘이 네 판에 걸쳐 「증명형을 견줄 앵커가 사다리에 하나도 없다」고 신고했다
%       ② 세 번째 판정에서 이 자리가 3.0 · 4.2 · 4.0 으로 갈렸다 (다른 열여섯은 폭이 0.2~0.4)
%       ③ 증명 답지는 경로를 채점해야 해 「어디까지 요구할지」를 문항마다 새로 정해야 한다
%    〔폐기한 것〕 1차본 = 대우+귀류로 √3 무리수 꼴 (2.97) · 2차본 = 「모두」의 부정과 3·5 갈림 (3.53).
%       둘 다 증명형이라 함께 접었다.
%
% 🔴 새 원본 — 와부고 2024 2학기 중간 서술형 2 (17번 · 5.0점)
%    「n(U)=30 · n(A)=x · n(B)=y 일 때 n(A∩B) 의 최댓값은 x, 최솟값은 10 → (x-15)y 의 최댓값」
%    아무 슬롯도 안 쓴 회차이고 앵커도 아니고 증명형도 아니다.
%
% 🔴 단원을 집합 쪽으로 정한 까닭 — 지금 서술형 13·15·16·17 이 **전부 좌표평면**이다.
%    14번까지 도형으로 가면 서술형 40점이 한 가지 근육만 잰다. **그것이 재현-1회에서 이미 일어난 일**이다
%    (1회 서술형 다섯이 전부 도형). 명제 쪽은 8·10·15번이 도구를 다 가져가 좁았다.
%
% 변형 셋 — 숫자·문자 교체 (x,y → a,b · 15번이 좌표 (x,y) 를 쓴다) · 소문항 둘로 가름 ·
%    🔴 **「최솟값은 10」을 「최댓값과 최솟값의 차가 14」로 뒤집었다.**
% 🔴 그 셋째가 갈래 둘을 세운다 —
%    갈래 ① m=0 (교집합이 빌 수 있다) → a=14 · b≤22 → a(b-a) 최대 **112**
%    갈래 ② m>0 → b=22 · 14<a≤22 → 최대 105  ← 미끼
%    🔴 소문항 (1) 은 m=6 이라 **갈래 ① 을 안 보여 준다.** (1) 의 식을 그대로 옮긴 학생은 105 를 적는다.
%    🔴 원본 꼴 (a-6)b 로 두면 갈래 ② 만 봐도 맞아 이 관문이 죽는다. 그래서 식을 a(b-a) 로 바꿨다.
% 🔴 번역 장치는 「M=a」다 — 학생이 이 한 마디를 「a ≤ b」로 옮겨야 한다. 문항이 안 알려 준다.
% 🔴 배점 2 : 6 (앞은 가볍게 · 합 8.0 불변)
% 답 (1) M=17 · m=6   (2) 112  — (a,b)=(14,22) 하나뿐 (a,b 를 1~36 전수로 확인 · 조건 만족 쌍 17개)
% 🔴 그림 없음 — 벤 다이어그램을 그리면 겹침이 가장 클 때와 가장 작을 때를 알려 준다
\begin{problem}{14}{s}
전체집합 $U$의 두 부분집합 $A$, $B$가
$$n(U)=36,\qquad n(A)=a,\qquad n(B)=b$$
% 🔴 2026-08-05 — 「원소의 개수가 위와 같은 두 집합 A, B 를 여러 가지로 잡을 때」를 뺐다 (마스터 지적 「표현이 어색해」).
%    M 과 m 이 무엇에 대한 최대·최소인지 못박으려고 덧댄 말인데, 없는 친절이었다.
%    「n(A∩B) 의 최댓값」은 한국 수학 교재에서 이미 「조건을 지키면서 두 집합을 어떻게 잡든」으로 읽힌다.
%    원본(2024 2학기 중간 17번)도 그 말 없이 「n(A∩B) 의 최댓값은 x」라고만 쓴다.
를 만족시킨다. $n(A\cap B)$의 최댓값을 $M$, 최솟값을 $m$이라 하자. 다음 물음에 답하시오. \cond{$a$, $b$는 자연수이다.} (각 과정을 논술할 것) \pts{8.0}
\end{problem}

\vspace*{4mm}
\textbf{(1)} $a=25$, $b=17$일 때, $M$과 $m$의 값을 각각 구하시오. [2점]
\vfill

\textbf{(2)} $M=a$이고 $M-m=14$일 때, $a(b-a)$의 최댓값을 구하시오. [6점]
\vfill\null
}
\end{pagepair}

% ══════════════════════════════════════════════════════════════════════════
% 페이지 5 (서술형 Q15 · 두 단원 융합) — 좌: 대발문+(1) / 우: (2)(3)
% 🔴 청사진이 마스터 확정으로 못박은 자리다 (S8 · 1회에 비어 있던 자리)
% ══════════════════════════════════════════════════════════════════════════
\begin{pagepair}
\probpairfillpage{%
% 원본 A: 와부고 2023 2학기 중간 19번 (서술형 4) — 충분조건 → 필요조건 순으로 두 번 되짚는 뼈대
% 원본 B: 와부고 2024 1학기 기말 17번 (논술형 2) — 원 위의 모든 점이 어떤 영역에 있을 조건
%   묶는 자리 — 「원 위의 모든 점이 영역 안에 있다」가 곧 「충분조건」이다.
%   원본 A 가 수직선에서 하던 일을 좌표평면으로 올렸다
%   변형 — 원의 계수 교체 (중심 (k+3, 4-k) · 반지름 √(k²+4)) · 정수 개수 세기를 범위로
%   답 (1) k=0 · 넓이 4π  (2) -5/6 < k < 3/2  (3) k=-3 또는 k=3
%   🔴 (3) 의 답 둘은 (2) 의 범위 밖이다 — (2) 가 후보를 넘겨주지 않는다
%   🔴 그림 없음 — k 가 움직이면 원판도 움직인다. 한 자리를 그리면 (2)(3) 의 답이 샌다
\begin{problem}{15}{s}
좌표평면 위의 점 $(x,\,y)$에 대한 세 조건 $p$, $q$, $r$가 다음과 같다.
\begin{conditions}
$p:x^{2}+y^{2}-2(k+3)x-2(4-k)y+k^{2}-2k+21\le 0$\\
$q:|x|+|y|=x+y$이고 $xy>0$\\
$r:x+y\le t$
\end{conditions}
세 조건 $p$, $q$, $r$의 진리집합을 각각 $P$, $Q$, $R$라 하자. 다음 물음에 답하시오. \cond{$k$, $t$는 실수이다.} (각 과정을 논술할 것) \pts{8.0}
\end{problem}

\vspace*{4mm}
\textbf{(1)} 집합 $P$가 좌표평면 위에 나타내는 영역의 넓이가 최소가 되도록 하는 $k$의 값과 그때의 넓이를 구하시오. [2점]
\vfill\null
}{%
\textbf{(2)} 조건 $p$가 조건 $q$이기 위한 충분조건이 되도록 하는 $k$의 값의 범위를 구하시오. [3점]
\vfill

\textbf{(3)} 조건 $r$가 조건 $p$이기 위한 필요조건이 되도록 하는 $t$의 최솟값이 $7+\sqrt{26}$일 때, $k$의 값을 모두 구하시오. [3점]
\vfill\null
}
\end{pagepair}

% ══════════════════════════════════════════════════════════════════════════
% 페이지 6 (서술형 Q16 · 두 원과 넓이의 최댓값) — 좌: 대발문+(1) / 우: (2)(3)
% ══════════════════════════════════════════════════════════════════════════
\begin{pagepair}
\probpairfillpage{%
% 원본: 와부고 2023 1학기 기말 19번 (논술형 4 · 삼각형 넓이의 최댓값)
%   변형 — 세 번째 원 C3 를 뺌 · 원 C2 의 중심을 못 박지 않고 움직이게 함 ·
%          후보를 줄여 주는 단서(「단, 점 S 는 선분 OP 위에 있지 않다」)를 없앰
%   답 (1) 2 < OP < 8  (2) √34  (3) 12  (이때 OP=4 · QR=6)
%   🔴 (2) 의 √34 는 미끼다. (3) 에 그대로 갖다 쓰면 넓이가 11.03 으로 최댓값이 아니다
%   🔴 학생이 스스로 만들 것 — O 에서 직선 QR 까지의 거리 h 가 가질 수 있는 범위(4 ≤ h < 5),
%      그리고 그 범위의 왼쪽 끝이 답을 준다는 것
%   🔴 그림 없음 — 배치가 움직이는 것이 이 문항의 핵심이라 한 배치를 그리면 최댓값 자리를 흘린다
% 🔴🔴 2026-08-05 세션 115 검수 반영 — 셋이 겹쳤다.
%    ① **14번과 같은 덫**이었다. 둘 다 8.0점 · 둘 다 마지막 소문항이 「최댓값」 ·
%       둘 다 「앞 소문항의 값을 그대로 옮기면 틀린다」가 관문이었다 (16번은 (2)의 √34).
%    ② **원 쏠림** — 이 자리가 두 원을 쓰는 유일한 곳이었다.
%    ③ 이 자리는 네 판 내내 한 번도 안 고쳤고 1회 16번(5.30)보다 0.53 낮았다.
%
% 🔴 원본을 갈았다 — 2023 1학기 기말 19번 → **2019 1학기 기말 17번** (서술형 2 · 7.0점)
%    「한 변의 길이가 √10 인 정사각형 ABCD · 두 꼭짓점 A, B 가 각각 y축·x축 위 · B(1,0) ·
%      직선 CD 의 방정식」 (원본 답 3x+y-13=0)
%    🟢 **두 원을 버렸다. 이 안에는 원이 아예 없다.**
%       「중심에서 잰 거리 vs 반지름」이 여섯 문항·41.6점에서 다섯 문항·33.6점으로 내려간다.
%       두 원을 쓰는 자리는 이 회차에서 0 이 된다.
%
% 변형 셋 — (가) **그림을 뺐다.** 원본은 그림이 정사각형의 자리를 하나로 못 박았다.
%              빼면 직선 AB 의 양쪽 두 자리가 열린다
%           (나) **B(1,0) 을 지웠다.** 원본이 주던 값 A(0,3)·B(1,0) 이 (3)의 **답**으로 뒤집힌다.
%              못 박는 조건은 「선분 BD 를 1:3 으로 내분하는 점이 y축 위에 있다」 하나뿐이다
%           (다) 소문항 셋으로 갈랐다 (2 : 2 : 4)
% 🔴 (3) 에서 갈래가 둘 선다 — D = (a, a+b) 이면 a=-3b 라 a,b>0 에 걸려 죽고,
%    D = (-a, a-b) 이면 a=3b 에서 b=1 · a=3 이 남는다. 갈래가 둘이라는 말이 발문에 없다.
% 🔴 14번과 덫이 갈린다 — 14번은 앞 소문항의 **값**이 함정이고 여기는 **대발문의 생김새**가 함정이다.
%    (1) 이 넘기는 것은 값이 아니라 관계식이라 그대로 옮겨도 틀릴 자리가 없다.
% 🟢 「최댓값」·「최솟값」·「모두 구하시오」를 하나도 안 쓴다 — 셋 다 「구하시오」다.
%    (이 회차에 「최댓값·최솟값」이 여섯 번, 「모두 구하시오」가 일곱 번 나온다는 지적을 받았다.)
% 답 (1) a²+b²=10  (2) √10/2  (3) D(-3, 2)
\begin{problem}{16}{s}
좌표평면 위에 한 변의 길이가 $\sqrt{10}$인 정사각형 $\mathrm{ABCD}$가 있다. 꼭짓점 $\mathrm{A}$는 $y$축 위에 있고 꼭짓점 $\mathrm{B}$는 $x$축 위에 있으며, 두 점 $\mathrm{A}$, $\mathrm{B}$의 좌표를 각각 $(0,\,a)$, $(b,\,0)$이라 하자. 다음 물음에 답하시오. \cond{$a>0$, $b>0$이다.} (각 과정을 논술할 것) \pts{8.0}
\end{problem}

\vspace*{4mm}
\textbf{(1)} $a$와 $b$가 만족시키는 관계식을 구하시오. [2점]
\vfill\null
}{%
\textbf{(2)} $a=b$일 때, 원점과 직선 $\mathrm{AB}$ 사이의 거리를 구하시오. [2점]
\vfill

\textbf{(3)} 선분 $\mathrm{BD}$를 $1:3$으로 내분하는 점이 $y$축 위에 있을 때, 점 $\mathrm{D}$의 좌표를 구하시오. [4점]
\vfill\null
}
\end{pagepair}

% ══════════════════════════════════════════════════════════════════════════
% 페이지 7 (서술형 Q17 · 실세계 지문형 정점) — 좌: 지문+대발문 / 우: (1)(2)(3)
% 🔴 이 회차의 유일한 지문형이다 (1회는 둘 · 🔒 마스터 확정 세션 114)
% ══════════════════════════════════════════════════════════════════════════
\begin{pagepair}
\probpairfillpage{%
% 원본: 와부고 2021 1학기 중간 17번 (서답형 · 두 점을 지나는 원의 방정식)
%   보조 원본: 같은 회차 15번 (부등식으로 범위를 잡는 층) · 같은 회차 8번 (지문 문체)
%   변형 — 원본이 대놓고 주던 두 번째 조건(「중심이 직선 y=x-2 위에 있고」)을
%          학생이 세워야 하는 조건으로 바꿈. (2) 는 접함 · (3) 은 포함
%   수학을 먼저 완성하고 소재를 나중에 입혔다 (청사진 narrative_slots.design_protocol)
%   🔴🔴 2026-08-05 세션 115 재설계 — 맹검 실측 4.93. **이 회차에 프리미엄이 하나도 없었다**
%      (1회 17번은 5.47). 이 회차의 정점이 1회의 15·16번 높이밖에 안 됐다.
%      까닭: 세 소문항이 같은 길 위에 있었다. 전부 중심 P(t, -t+4) 하나를 세우고 시작해
%            그 뒤로 갈리는 것이 등식이냐 부등식이냐뿐이었다. 학생이 틀릴 자리가 없었다.
%   🔴 원본은 그대로다 (2021 1학기 중간 17번). 지문도 그대로다. 소문항 (2)·(3) 만 갈았다.
%
%   (2) 를 뒤집었다 — 도달 거리를 묻는 대신 스피커 자리를 주고 들리는 구간의 길이를 묻는다.
%      |-t+4| = 1 에서 t=3 또는 t=5 인데 🔴 **t=5 는 중심이 큰길 남쪽(y=-1)이다.**
%      발문에 방향이 없어 학생이 남쪽 자리를 스스로 살려야 한다.
%      답이 400√2 와 400√7 로 아주 달라 한쪽을 찾아도 다른 쪽은 처음부터 다시 센다.
%      덤 — 접함이 사라져 12번(교점 개수)과의 겹침이 하나 줄었다.
%
%   (3) 에 문자를 살렸다 — 구간의 동쪽 끝을 a 로 두면 하한 둘 중 어느 쪽이 이기는지가 **뒤바뀐다.**
%      (k²+7)/(2k+2) - 7/2 = k(k-7)/(2k+2) 를 학생이 만들어야 경계 a=700 이 나온다.
%      🔴 갈래가 둘이라는 것도, 경계가 어디라는 것도 발문에 없다.
%
%   답 (1) A(0,1) · B(3,4) · 300√2 m
%      (2) 400√2 m 또는 400√7 m
%      (3) 0 < a ≤ 700 · 도달 거리 250√2 m
%   🔴 근호 답을 정수로 다듬지 않았다 (페르소나 §6-5). 이중근호는 없다
%   🔴 (2) 가 (3) 에 후보를 넘겨주지 않는다 — (3) 의 t=7/2 는 (2) 의 t=3·5 와 무관하다
%   🔴 그림 없음 — 큰길과 두 표지를 그리면 (2) 에서 스피커가 큰길 남쪽에도 앉는 것이 눈으로 보인다
\begin{problem}{17}{s}
다음 글을 읽고 물음에 답하시오. (각 과정을 논술할 것) \pts{10.0}
% 🔴🔴 2026-08-05 세션 115 — 문단마다 붙어 있던 소제목 배지 넷을 뺐다 (마스터 지적).
%    「[마을의 모습]」·「[두 표지의 자리]」·「[말의 뜻]」·「[정할 것]」 이었다.
%    🔴 왜 뺐나 — **있어야 할 이유를 못 댔다.** 1회 16·17번이 그렇게 되어 있어 따라 한 것이다.
%    🔴 오히려 해롭다 — 「[말의 뜻]」이 여기 정의가 있다고, 「[정할 것]」이 여기가 조건이라고 알려 준다.
%       지문형의 부담 축은 「무슨 말인지 알아내는 데 품이 든다」인데 배지가 그 품을 덜어 준다.
%    🔴 대신 가) 나) 다) 라) 를 단다 (마스터 제안 2026-08-05).
%       **문단은 갈라 주되 내용은 안 알려 준다** — 「다)」는 거기 정의가 있다는 것을 흘리지 않는다.
%       굵게 하지 않는다. 조건 상자의 「(가)」와 헷갈리지 않게 여는 괄호 없는 꼴로 둔다.
%    🔴 정의되는 낱말의 굵은 글씨는 남긴다 — 그것은 「이 말을 지금 정의한다」는 표시라 교과서 관례이고,
%       어디를 봐야 하는지 알려 주는 것이 아니다.
\begin{condbox}
{\small
가) 어느 마을에 재난 안내 방송을 하는 스피커를 새로 세우려고 한다. 마을 앞에는 큰길이 동서 방향으로 곧게 나 있다. 큰길가의 표지석을 원점, 큰길을 $x$축, 북쪽을 $y$축의 양의 방향으로 하는 좌표평면을 잡고, 좌표평면의 한 눈금을 $100\,\mathrm{m}$로 한다.
\par\vspace{8mm}
나) 마을에는 표지 $\mathrm{A}$와 표지 $\mathrm{B}$가 있다. 표지 $\mathrm{A}$는 표지석에서 북쪽으로 $100\,\mathrm{m}$ 떨어진 곳에 있고, 표지 $\mathrm{B}$는 표지 $\mathrm{A}$에서 동쪽으로 $300\,\mathrm{m}$, 북쪽으로 $300\,\mathrm{m}$ 떨어진 곳에 있다.
\par\vspace{8mm}
다) 스피커에서 잰 거리가 도달 거리 이하인 지점 전체를 \textbf{들리는 구역}이라 한다. 또 스피커에서 잰 거리가 도달 거리와 꼭 같은 지점에서는 방송이 \textbf{겨우 들린다}고 한다.
\par\vspace{8mm}
라) 스피커는 좌표평면 위 어디에나 세울 수 있고, 도달 거리는 출력에 따라 정한다. 두 표지 $\mathrm{A}$, $\mathrm{B}$에서 방송이 모두 겨우 들리도록 스피커의 자리와 출력을 정하려고 한다. 소리를 막는 것은 없다고 본다.
}
\end{condbox}
\end{problem}

% 🔴 조판 배치 — 좌: 지문 + 소문항 (1) / 우: (2)(3)
%    1회 16번의 v4 이전 배치와 같다. 1회는 지문이 674자로 길어져 소문항 셋을 다 우측으로 옮겼는데,
%    이 지문은 그보다 짧아 좌 컬럼이 80mm 넘게 비었다 (1차 실측). 그 자리를 (1) 의 풀이 공간으로 쓴다.
%    🔴 지문 안 블록 사이는 8mm 로 둔다 — 10mm 로 두면 12mm 넘게 렌더링돼
%       실측 도구가 그것을 「풀이 공간」으로 세어 컬럼이 균등하지 않다고 잡는다.
\vspace*{4mm}
\textbf{(1)} 두 표지 $\mathrm{A}$, $\mathrm{B}$의 좌표를 각각 구하고, 두 표지 사이의 거리를 구하시오. [2점]
\vfill\null
}{%
\textbf{(2)} 스피커를 큰길에서 $100\,\mathrm{m}$ 떨어진 곳에 세웠다. 이때 큰길에서 방송이 들리는 구간의 길이로 가능한 값을 모두 구하시오. [4점]
\vfill

\textbf{(3)} 표지석에서 동쪽으로 $a\,\mathrm{m}$ 떨어진 지점을 표지 $\mathrm{C}$라 하자. 큰길 위에서 표지석부터 표지 $\mathrm{C}$까지의 구간 전체에 방송이 들리도록 하되, 도달 거리는 가장 짧게 잡는다. 이 도달 거리가 $a$의 값이 달라져도 변하지 않는 $a$의 값의 범위를 구하고, 그때의 도달 거리를 구하시오. [4점]
% 🔴 실측 보정 (scripts/풀이공간-실측.py) — 90.0 / 103.6mm 로 비가 1.15 였다 (한계 1.15).
%    남는 자리는 언제나 마지막 칸으로 몰린다. 맨 아래에 (마지막 − 첫) = 13.6mm 를 음수로 두어
%    그만큼을 두 칸이 나눠 갖게 한다. \vfill 숫자를 맞추는 것으로는 안 된다 (1회 6·7쪽과 같은 손질).
\vspace*{-13.6mm}
\vfill\null
}
\end{pagepair}

\end{document}
