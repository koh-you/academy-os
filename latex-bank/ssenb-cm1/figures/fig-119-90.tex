% 119-90(119쪽) — 같은 간격으로 놓인 12개의 점(가로 4개 · 세로 3개). 스캔 화질이 낮아 TikZ 로 다시 그림.
% 점의 배치만 원문 그대로. 이은 선·라벨은 원문에 없다.
\begin{tikzpicture}[scale=0.62, line width=0.6pt]
  \foreach \x in {0,1,2,3} { \foreach \y in {0,1,2} { \fill (\x,\y) circle (2.2pt); } }
\end{tikzpicture}
