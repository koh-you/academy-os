# 쎈B 미적분1 · LaTeX 전사 가이드 (items.json 작성 규약)

확인일: 2026-09-26. **스캔 PDF**(글자 레이어 없음, 128쪽)이고, 문항 번호는 **중단원마다 01 부터 다시 시작**한다. 이 책은 **배지가 두 자리까지만** 간다(단원 최대 85 · 쪽 렌더로 9개 단원 마지막 쪽을 전부 확인). id 는 `쪽-번호`(예 `73-69`)이고 items.json 은 `id_style` 을 쓰지 않는다(쎈B 공통수학1 과 같은 방식). 출처 배지는 「쎈B 미적분1 73쪽 69번」으로 찍히므로 문항마다 `page`(인쇄 쪽)를 적는다.

교재는 **쎈의 B단계 유형 학습서**다. 「유형 뽀개기」 구역이 본체이고, 유형 상자 바로 아래 「대표 문제」가 오고 그 뒤로 같은 유형의 문항이 이어진다.

## 입력

- 문항 크롭 `output/problem-bank/ssenb-calc1/items/pbk_ba2f85c043-<쪽>-<번호>.jpg` (220 dpi).
- 쪽 전체 `output/problem-bank/ssenb-calc1/qa/pNNN.jpg` — 구역·유형 제목·태그·난이도·연계 쎈 번호는 **여기서** 읽는다. 문항 크롭만 보고 판단하지 않는다.
- 인쇄 쪽 = pdf 쪽 (offset 0). 차례는 pdf 4쪽, 대단원 표지는 pdf 5·29·91쪽.
- 답·해설은 이 배치에 없다. `answer` 는 **빈 문자열**로 두고 `answer_source: ""` 로 남긴다 — 답지(`C:/Users/PC/Desktop/교재 라텍스 조판/쎈B고등미적분1_정답.pdf`)는 전사 뒤 별도 ingest 로 붙인다.

## 단원과 문항 쪽 (쪽 렌더로 검증함)

차례의 쪽 번호는 **개념정리 쪽**을 가리킨다. 실제 문항은 그 다음 쪽부터다.

| 단원 | 개념정리 쪽 | 문항 쪽 | 첫 문항 연계번호 | 문항 수 |
|---|---|---|---|---|
| 01 함수의 극한 | 6 | 7~17 | 쎈 0044 | 71 |
| 02 함수의 연속 | 18 | 19~28 | 쎈 0172 | 53 |
| 03 미분계수와 도함수 | 30 | 31~43 | 쎈 0274 | 81 |
| 04 도함수의 활용 (1) | 44 | 45~58 | 쎈 0398 | 84 |
| 05 도함수의 활용 (2) | 60 | 61~75 | 쎈 0532 | 85 |
| 06 도함수의 활용 (3) | 76 | 77~89 | 쎈 0646 | 76 |
| 07 부정적분 | 92 | 93~100 | 쎈 0761 | 53 |
| 08 정적분 | 102 | 103~115 | 쎈 0869 | 83 |
| 09 정적분의 활용 | 116 | 117~128 | 쎈 0983 | 73 |

합계 **659문항**. 문항이 없는 쪽: 5·29·91(대단원 표지) · 6·18·30·44·60·76·92·102·116(개념정리) · 59·90·101(「나를 바꾸는 힘」 삽화 쪽).

## 놓친 문항을 채운다 (중요)

스캔 검출기가 **「01 | 대표 문제」처럼 검은/색 상자 안 흰 숫자**를 못 읽어 크롭이 없는 문항이 있다. 배치 목록에 번호가 비어 있으면 **원본 PDF 쪽 렌더(`qa/pNNN.jpg`)에서 직접 전사해 넣는다.** 빠뜨리면 그대로 사라진다.

### 무엇이 참인가 — 배지 번호와 쎈 연계번호

**배치 목록·크롭 파일 이름의 번호를 믿지 말 것.** 검출기가 배지를 못 읽거나 본문 숫자를 배지로 잘못 읽으면 그 자리에서 순번이 밀려 이후 번호가 통째로 어긋난다. 이 책에서도 실측으로 확인했다 — pdf 73쪽에서 실제 배지는 69·70·71·72·73·74인데 검출기는 본문에서 「22」를 배지로 잘못 읽어 69 뒤를 22·23·24…로 매겼다.

**id 의 뒷자리는 언제나 쪽 이미지에서 읽은 배지 번호다.** 크롭 파일 이름과 다르면 배지 번호를 쓰고 `review_needed` 에 「번호 불일치: 크롭 <파일번호> → 배지 <실제>」로 적는다.

**누락 판정은 「쎈 NNNN」 연계번호로 한다.** 이게 가장 확실한 기준이다 — 문항마다 오른쪽 위에 있고 **한 중단원 안에서는 정확히 1씩 증가한다**(9개 단원 전부 첫 문항·마지막 문항으로 검산함: 예컨대 05단원은 01번이 쎈 0532, 85번이 쎈 0616 = 0532+84). 단원이 바뀌면 건너뛰므로(예: 01단원 끝 0114 → 02단원 첫 0172) **단원을 넘어서는 연속성은 보지 않는다.**

담당 쪽의 연계번호를 순서대로 적어 보고 **끊긴 자리가 있으면 그 자리에 문항이 있다**는 뜻이니 쪽 이미지에서 찾아 채운다. 같은 단원 안의 앞 배치 마지막 연계번호와도 이어지는지 본다. 배지 번호 − 1 = 연계번호 − (그 단원 첫 연계번호) 가 항상 성립한다.

### 문항이 아닌 쪽

- **대단원 표지**(pdf 5·29·91쪽): 「Ⅱ 다항함수의 미분법」 + 중단원별 유형 목차. 「유형 01」…이 줄줄이 있어 검출기가 문항으로 잡기 쉽다.
- **개념정리 쪽**(pdf 6·18·30·44·60·76·92·102·116쪽): 「03·1 평균변화율」 같은 개념 상자만 있다. 빈칸 채우기 번호 ①②를 문항으로 잡은 것이 섞인다.
- **삽화 쪽**(pdf 59·90·101쪽): 「나를 바꾸는 힘」 만화·사진. 문항 없음.

이 쪽의 검출 결과는 전사하지 않고 `review_needed` 에 「문항 아님 — <표지/개념정리/삽화> 쪽 오검출」로 적는다. (배치 목록에는 이 쪽을 아예 넣지 않았으므로 정상 작업에서는 마주치지 않는다.)

### 배치 목록의 `⚠ number_corrected(N)` 는 「OCR 은 N 으로 읽었다」는 뜻이다

ingest 는 앞 쪽에서 배지 하나를 놓치면 그 뒤 번호를 **순번에 맞춰 덮어쓴다.** 덮어쓸 때 원래 읽은 값을 `number_corrected(N)` 로 남긴다. 이 책에서는 **연계번호로 검산한 결과 거의 언제나 N 이 맞고 파일 이름이 틀렸다.** 배치 목록에 `⚠ number_corrected(N)` 가 보이면 N 을 배지 후보로 삼고 쪽 이미지로 확정한다.

### 크롭 라벨이 실제 배지와 어긋나는 구간 (연계번호로 검산함 · 총 38문항)

| 쪽 | 크롭 파일 번호 | 실제 배지 |
|---|---|---|
| 55 | 60~65 | **61~66** |
| 56 | 66~70 | **67~71** |
| 56 | 15 | **72** |
| 57 | 16, 17 | **73, 74** |
| 58 | 18~23 | **79~84** |
| 73 | 23, 24, 25 | **70, 71, 72** |
| 73 | 27, 28 | **73, 74** |
| 74 | 29~34 | **75~80** |
| 75 | 35~39 | **81~85** |

그 밖의 쪽은 크롭 파일 번호 = 실제 배지다. 어긋난 것을 쓸 때는 `review_needed` 에 「번호 불일치: 크롭 <파일번호> → 배지 <실제>」로 적는다.

### 문항이 아닌 크롭 (전사하지 말 것 · 10개)

본문 숫자·선택지를 배지로 잘못 읽은 것이다. 모두 쪽 안에서 번호 순서를 거스르는 자리에 있어 쪽 이미지로 바로 확인된다.

`21-22` `56-14` `73-22` `73-26` `80-25` `84-23` `96-29` `119-22` `121-14` `123-18`

(`73-26` 과 `80-25` 는 바로 앞 크롭과 같은 배지를 두 번 잡은 것이다.)

### 크롭이 없는 문항 (쪽 이미지에서 전사해 채울 것 · 11개)

| 단원 | 빠진 배지 | 있는 쪽 |
|---|---|---|
| 03 미분계수와 도함수 | 16 | 33 |
| 03 미분계수와 도함수 | 43 | 38 |
| 04 도함수의 활용 (1) | 60 | 54 |
| 04 도함수의 활용 (1) | 75, 76, 77, 78 | 57 |
| 07 부정적분 | 01, 05 | 93 |
| 07 부정적분 | 25 | 96 |
| 09 정적분의 활용 | 47 | 124 |

01·02·05·06·08 단원은 빠진 문항이 없다(검출 수 = 연계번호로 센 실제 수). 채워 넣은 문항은 `review_needed` 에 「크롭 없음 — 쪽 이미지에서 전사」라고 적는다.

### 유형 상자 제목은 쪽 이미지에서 읽는다

이 책의 유형 라벨은 「유형 01」처럼 **두 자리**라 검출기가 유형 상자를 거의 잡지 못했다(`type_label` 이 대부분 비어 있다). 그래서 유형 상자 바로 앞 문항의 크롭이 **다음 유형 상자까지 물고 내려올 수 있다.** 그룹(`section`)은 반드시 쪽 이미지에서 읽고, 크롭 아래쪽에 유형 상자가 섞여 들어와 있으면 그 부분은 전사하지 않는다.

## 배치 목록 (`work/batch-<이름>.md` · 18개)

단원을 가로지르는 배치는 없다. 한 쪽 안에서 단원이 바뀌는 경계 쪽도 없다 — 단원은 언제나 개념정리 쪽에서 바뀌고 그 쪽에는 문항이 없다.

| 배치 | 인쇄 쪽 | 단원 | 크롭 수 |
|---|---|---|---|
| A | 7~12 | 01 함수의 극한 | 38 |
| B | 13~17 | 01 함수의 극한 | 33 |
| C | 19~23 | 02 함수의 연속 | 26 |
| D | 24~28 | 02 함수의 연속 | 28 |
| E | 31~37 | 03 미분계수와 도함수 | 39 |
| F | 38~43 | 03 미분계수와 도함수 | 40 |
| G | 45~51 | 04 도함수의 활용 (1) | 42 |
| H | 52~58 | 04 도함수의 활용 (1) | 38 ← **번호 어긋남 구간(55~58쪽)** |
| I | 61~67 | 05 도함수의 활용 (2) | 41 |
| J | 68~75 | 05 도함수의 활용 (2) | 46 ← **번호 어긋남 구간(73~75쪽)** |
| K | 77~83 | 06 도함수의 활용 (3) | 43 |
| L | 84~89 | 06 도함수의 활용 (3) | 35 |
| M | 93~96 | 07 부정적분 | 25 |
| N | 97~100 | 07 부정적분 | 26 |
| O | 103~109 | 08 정적분 | 45 |
| P | 110~115 | 08 정적분 | 38 |
| Q | 117~122 | 09 정적분의 활용 | 39 |
| R | 123~128 | 09 정적분의 활용 | 36 |

합 658크롭 = 실제 659문항 − 크롭 없는 11 + 오검출 10.

병합은 `node scripts/latex-bank/merge-batches.mjs --work latex-bank/ssenb-calc1/work --bank latex-bank/ssenb-calc1 --map "A:01:함수의 극한:7~17,B:01:함수의 극한:7~17,C:02:함수의 연속:19~28,D:02:함수의 연속:19~28,E:03:미분계수와 도함수:31~43,F:03:미분계수와 도함수:31~43,G:04:도함수의 활용 (1):45~58,H:04:도함수의 활용 (1):45~58,I:05:도함수의 활용 (2):61~75,J:05:도함수의 활용 (2):61~75,K:06:도함수의 활용 (3):77~89,L:06:도함수의 활용 (3):77~89,M:07:부정적분:93~100,N:07:부정적분:93~100,O:08:정적분:103~115,P:08:정적분:103~115,Q:09:정적분의 활용:117~128,R:09:정적분의 활용:117~128"` 로 한다.

## items.json 구조

```json
{
  "book": "쎈B 미적분1", "variant_level": 0,
  "units": [ { "code": "01", "title": "함수의 극한", "pages": "7~17", "groups": [
      { "id": "U1-A1", "section": "유형 01 함수의 극한값의 존재", "items": ["7-01", "7-02"] },
      { "id": "U1-A2", "section": "유형 02 함수의 극한값 구하기", "items": ["8-06"] }
  ] } ],
  "items": { "7-01": { "page": 7, "body": "…", "answer": "", "answer_source": "" } }
}
```

- 그룹 id: `U<단원>-<배치이름><n>` (예 `U1-A1`). 배치 안에서 1부터.
- 그룹 = 「유형 상자 하나」 또는 「공통 지시문 하나」. 유형 상자의 설명 글(연한 바탕)은 전사하지 않는다 — `section` 제목만 쓴다.
- `section`: 유형 상자의 「유형 01 제목」 그대로. 제목이 두 줄이면 원문의 `;` 까지 한 줄로 잇는다(예 「유형 16 정적분으로 정의된 함수의 극한; $\lim\limits_{x\to a}\frac{1}{x-a}\int_a^x f(t)dt$ 꼴」).

## 문항 필드

| 필드 | 뜻 |
|---|---|
| `page` | 인쇄 쪽. 필수. |
| `body` | 발문 LaTeX. 지시문은 넣지 않는다(그룹 `passage`). |
| `figure` | `"tikz:fig-<id>"` 또는 `"crop:fig-<id>.jpg"`. 원문에 그림·표가 있을 때만. |
| `figure_layout` | 생략 = 옆. 넓은 표·그래프는 `"below"`. |
| `choices` | 5지선다(각 `$…$`). `choices_layout` 생략 = 3-2, `"ii"` = 2-2-1, `"v"` = 세로. |
| `subs` | 소문항 `(1)(2)…`. |
| `tag` | 번호 옆 표시가 있을 때만 — 「대표 문제」「서술형」「집중공략」. |
| `level` | 번호 오른쪽 난이도 동그라미(하·중·상)가 있으면 원문 그대로. |
| `ssen_ref` | 오른쪽 위 「쎈 0532」 연계 번호. 있으면 숫자만(`"0532"`). |
| `answer` | **이 배치에서는 빈 문자열**. |
| `answer_source` | **빈 문자열**. |

## 표기 규약 (고2 미적분1 · 함수의 극한과 연속 · 다항함수의 미분법 · 다항함수의 적분법)

- 극한: `$\displaystyle\lim_{x\to 2} f(x)$`(`\displaystyle` 붙여 아래첨자가 아래에), `$x\to 0+$`, `$x\to 2-$`, `$x\to\infty$`, `$x\to-\infty$`. 「$f(x)$가 $x=1$에서 연속」은 한글. 좌극한·우극한·불연속·수렴·발산은 한글. 부정형은 `$\dfrac{0}{0}$ 꼴`·`$\dfrac{\infty}{\infty}$ 꼴`·`$\infty-\infty$ 꼴`·`$\infty\times 0$ 꼴`처럼 「꼴」을 붙인다.
- 연속·정리: 사잇값 정리·최대·최소 정리·평균값 정리·롤의 정리는 한글. 구간은 `$[-2,\,3]$`·`$(a,\,b)$`, 「닫힌구간 $[0,\,2]$」처럼 한글 + 기호.
- 미분: `$f'(x)$`, `$f'(a)$`, `$f''(x)$`, `$y'$`, `$\dfrac{dy}{dx}$`, `$\dfrac{d}{dx}f(x)$`. 미분계수·도함수·접선의 기울기·미분가능·증분은 한글. 평균변화율은 `$\dfrac{\Delta y}{\Delta x}$`. 증가·감소·극대·극소·극값·변곡은 한글, 「$f'(x)=0$」 그대로. 속도 `$v(t)$`·가속도 `$a(t)$`, 변화율 `$\dfrac{dV}{dt}$`.
- 적분: `$\displaystyle\int f(x)\,dx$`, `$\displaystyle\int_{0}^{1} x^2\,dx$`(`\,dx`), `$\displaystyle\int_{a}^{b} f(x)\,dx=\Big[F(x)\Big]_{a}^{b}$`, 적분상수 `$C$`. 부정적분·정적분·넓이·부피는 한글. 합 `$\displaystyle\sum_{k=1}^{n}$`.
- 함수·그래프: `$f(x)=x^3-3x^2+2$`, `$y=f(x)$의 그래프`, 「$x$축」 `$x$축`, 「$y$좌표」 `$y$좌표`. 절댓값 `$|x|$`, 가우스 `$[x]$`, 합성함수 `$(f\circ g)(x)$`, 근호 `$\sqrt{3x}$`. 그래프·좌표평면은 그림.
- 도형·단위: `$\seg{AB}$`, `$\angle\pt{B}=45^\circ$`, `삼각형~$\pt{ABC}$`, `$8\,\mathrm{cm}$`, `$20\,\mathrm{km/h}$`, `$\mathrm{cm}^3/\mathrm{s}$`. 「점~$\pt{P}$」처럼 명사 + 기호는 `~`. 원점은 `$\pt{O}$`.
- 분수 `\dfrac`, 좌표 쉼표 뒤 `\,`(`$(2,\,1)$`). 「(단, …)」 → `\cond{…}`. 본문 수식은 `$…$` 만(`$$`·`\[` 금지). 줄바꿈은 손으로 넣지 않는다. 원문의 별도 줄 식은 `\par\smallskip\centerline{$…$}\par\smallskip\noindent`.
- 보기 「ㄱ, ㄴ, ㄷ」 상자는 body 끝에 `\bogi{ㄱ. …\\ ㄴ. …\\ ㄷ. …}`. 조건 ㈎㈏ 상자는 `\exprbox{…}`. 범위 물결은 `$\sim$`.
- **원문의 수치·조건·보기 순서·소문항 수를 바꾸지 않는다.** 크롭에 워터마크가 겹쳐 있어도 원문 글자만 전사한다.

## 그림

- 좌표평면·수직선·그래프·도형은 TikZ 로 다시 그린다(`figures/fig-<id>.tex`). 정점 2.2pt · 축 0.7pt · 곡선 0.5~0.6pt, 라벨은 원문 것만, **답이 그림에서 읽히지 않게**(예: 넓이를 묻는 문항의 색칠 영역에 수치를 넣지 않는다). 첫 줄 주석에 「<id>(쪽) — 무엇 · 다시 그린 까닭」.
- 이 책은 $y=f'(x)$ 그래프·속도 $v(t)$ 그래프가 많다. 원문의 교점 표시·점선·표시된 문자($\alpha$, $\beta$, $a$, $b$)만 옮기고 없는 눈금을 만들지 않는다.
- 실생활 삽화·사진(원뿔·풍선·상자 그림 등)은 크롭: `node scripts/latex-bank/crop-figure.mjs --pdf "C:/Users/PC/Desktop/교재 라텍스 조판/쎈B 미적분1.pdf" --page <쪽> --item <manifest bbox_normalized> --rel <문항 안 비율> --out latex-bank/ssenb-calc1/figures/fig-<id>.jpg`.
- 원문에 없는 그림은 넣지 않는다.

## 확정 절차

1. 쪽 이미지(`qa/pNNN.jpg`)로 구역·유형 제목·태그·난이도를 파악해 그룹을 만든다.
2. 문항 크롭을 하나씩 읽어 `body`/`choices`/`subs` 를 전사한다. 배지 번호가 id 의 뒷자리.
3. **담당 쪽의 연계번호를 순서대로 적어 끊긴 자리를 찾고, 그 문항을 쪽 이미지에서 전사해 채운다.** 크롭이 없으면 `review_needed` 에 「크롭 없음 — 쪽 이미지에서 전사」라고 적는다.
4. `node scripts/latex-bank/check-batch.mjs <out.json> --bank latex-bank/ssenb-calc1` 로 컴파일 확인.
5. 확실치 않은 것은 `review_needed` 에 적는다. 답은 이 단계에서 채우지 않는다.
