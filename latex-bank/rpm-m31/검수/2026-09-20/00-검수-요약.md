# RPM 중학 3-1 전사 정합 검수 · 2026-09-20

- 대상: 1133문항 전수(ids-01~15 · 76문항씩 · Sonnet 검수 에이전트 15개). 원본 크롭(텍스트 PDF) ↔ `review/<id>.png` 조판본 대조.
- 결과: checked 1133 · ok 1124 · red 6 · yellow 3 · info 3.
- 내용 불일치(수치·부호·보기·소문항): **0건**. red 6건은 모두 같은 조판 잘림 한 가지.

## red (전부 수정)

| id | 원인 | 조치 |
| --- | --- | --- |
| 1022·1023·1024·1025·1028·1029 | 지시문 `$y=ax^2+bx+c$` 가 좁은 낱장 폭에서 overfull 로 오른쪽이 잘림(수식 안 줄바꿈 금지 규약 + 앞 줄이 tolerance 밖). items.json 본문은 정상 | `build.mjs` 프리앰블에 `\emergencystretch=2em` 추가 → 수식 앞에서 줄이 바뀌어 전부 표시. 재렌더 확인 |

## yellow (전부 수정)

| id | 원인 | 조치 |
| --- | --- | --- |
| 0526·0527 | 첫 줄이 `aligned` 3행이라 출처 배지가 첫 줄 수식과 겹침 | `badgeRaiseMm` 에 `aligned` 추가(5.5mm). 재렌더 확인 |
| 0979 | 그림 옆 좁은 폭에서 `+\dfrac{q}{a}` 앞 줄바꿈이 어색 | 본문 `{+\dfrac{q}{a}}` 로 묶음 + emergencystretch. 재렌더 확인 |

## info (답 대조)

- 0347(②)·0693(⑤)·0759(2)·1109((6, 2)): 답 크롭이 없어 전사자가 계산/해설로 넣은 답 — 검수 에이전트가 해설 쪽 렌더로 재대조, 전부 일치.

## 전사 단계 후처리(검수 전 반영)

- 자동 크롭 누락·잘림 15건 재크롭: 그룹 U2-E4(0160~0163 수직선), 0188·0189·0190·0210·0211·0220(수직선), 0858(점 삼각형), 0340·0341·0582·0583·0584·0928(잘림), 0389(머리띠 제외).
- 전사 review_needed 154건은 `전사-review-needed.json` — 대부분 「상자 크롭을 \bogi/\exprbox 로 대체」「머리띠 크롭 미사용」「page 를 pdf−3 인쇄 쪽으로 기재」 메모.

## build.mjs 변경

- `--only <id,…>`: `--review` 에서 지정 문항만 다시 렌더(export 와 함께 쓰면 오류).
- `\emergencystretch=2em`, badgeRaiseMm `aligned`.
