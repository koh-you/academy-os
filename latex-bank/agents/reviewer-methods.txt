MATH::각 문항을 passage + body(+subs·choices·hint 빈칸·figure 의 TikZ 좌표)만 보고 **직접 풀이**한다(answer 를 먼저 보지 않는다). 계산은 Python(`fractions.Fraction`·정수 제곱근 분해) 또는 Node 로 되대입 검산한다. 그 뒤 items.json 의 `answer` 와 대조: 값·선택지 번호(보기 값과 대조)·소문항 개수·힌트 빈칸 순서·「모두 구하시오」의 해 개수. `answer_source` 가 「계산」인 문항은 2중 검산(정방향 + 되대입). 답이 「풀이 참조」인 그리기 문항은 answer 의 설명이 맞는지 확인. 불일치는 🔴, 발문만으로 답이 정해지지 않거나 애매하면 🟡.
TRANS::`latex-bank/ssen-basic-cm2/review/<id>.png` 를 Read 로 열어 **왼쪽 원본 크롭 ↔ 오른쪽 조판본**을 눈으로 대조한다(전수). 확인 항목: 숫자·부호·좌표·지수·근호·분수, 문장(조사·「(단, …)」·「모두」·「양수」 등 조건어), 소문항 개수, 선택지 5개 값·순서, 힌트 빈칸 개수·위치, 태그(꼭 나와요·서답형·서술형), 그림(원문에 있는 요소가 다 있는지 · 라벨 겹침 · 답 노출), 조판(수식 중간 줄바꿈·글자 겹침·잘림·빈 문항). 원문과 다른 값·문장은 🔴, 조판 경미(줄바꿈·간격)는 🟡. 원본 크롭이 잘려 판독 불가면 쪽 이미지 `output/problem-bank/ssen-basic-cm2/qa/p0NN.jpg`(pdf 쪽 = 인쇄 쪽 − 6)로 본다.
POLICY::(1) 조판 정책: `latex-bank/ssen-basic-cm2/build/book.log` 의 Overfull/Underfull, `items/*.tex` 에서 `$$`·`\[`·수식 안 줄바꿈 후보(`\` 안 `$`), `\cond{}`·`점~$\pt{P}$` 규약 위반(「(단,」이 본문에 직접 있거나 「점 P」처럼 `~` 없는 것), `\dfrac` 대신 `\frac`, 선택지 환경(분수 좌표 5개인데 choices32 등). (2) 그림 규칙: `figures/*.tex` 전수 — 정점 2.2pt·파생점 2pt·축 0.7pt·눈금 없음·라벨 겹침(좌표로 판단)·원문에 없는 값·답 노출·머리 주석. 그림이 있는 문항은 `build/book.pdf` 해당 쪽을 `node scripts/latex-bank/render-pages.mjs latex-bank/ssen-basic-cm2/build/book.pdf <outDir> <쪽>` 으로 렌더해 Read 로 확인한다(임시 폴더는 `C:\Users\PC\AppData\Local\Temp\claude\C--Users-PC-github-academy-os\d5afe4fe-a519-4260-b3c9-4df2b2235b12\scratchpad\review-render`). (3) 구조: 그룹 section 이름이 차례·유형 라벨 체계(개념 NN 제목 · 유형 NNN 제목 · 실전 감각 UP)와 맞는지, 유형 번호가 책 순서로 이어지는지(2단원 010~022 · 3단원 023~039), 같은 지시문이 그룹 경계에서 중복되는지, 문항 순서가 책 순서인지, id 가 manifest(`output/problem-bank/ssen-basic-cm2/manifest.json` number_label)와 1:1 인지(빠짐·중복). (4) 고아 제목·지시문(쪽 끝에 제목만 남는 것)은 book.pdf 전 쪽을 렌더해 본다.
