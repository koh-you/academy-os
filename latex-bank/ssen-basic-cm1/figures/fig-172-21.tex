% 실전 감각 UP 172-21(본책 172쪽) — 직사각형 안에 가로에 평행한 선분 4개·세로에 평행한 선분 3개를 그은 격자(가로 4칸 × 세로 5칸). 스캔 그림을 TikZ 로 다시 그림. 라벨·수치 없음.
\begin{tikzpicture}[scale=0.62, line width=0.55pt]
  \draw (0,0) rectangle (2.4,3);
  \foreach \y in {0.6,1.2,1.8,2.4} { \draw (0,\y) -- (2.4,\y); }
  \foreach \x in {0.6,1.2,1.8} { \draw (\x,0) -- (\x,3); }
\end{tikzpicture}
