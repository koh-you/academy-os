% 개념 64 166-38~40 그룹 공통 그림(본책 166쪽) — 원 위의 5개의 점(라벨 없음). 스캔 그림을 TikZ 로 다시 그림. 점은 원둘레에 고르게(정오각형 자리) 두고 값은 적지 않음.
\begin{tikzpicture}[line width=0.5pt]
  \draw (0,0) circle (1.1);
  \foreach \a in {90,162,234,306,18} { \fill (\a:1.1) circle (2pt); }
\end{tikzpicture}
