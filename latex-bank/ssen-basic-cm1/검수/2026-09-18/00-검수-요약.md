# 베이직쎈 공통수학1 LaTeX 전사 검수 요약 · 2026-09-18

전사 정합 검수(원본 스캔 크롭 ↔ 조판본 review/*.png · 크롭이 없거나 잘린 문항은 쪽 이미지 qa/p<쪽>.jpg) — 검수 에이전트 20개(79문항씩) · 확인 1570/1570 · 이상 없음 1518 · 🔴 1 · 🟡 51.

수치·부호·조건·보기 값·소문항 수의 **내용 불일치는 0건**(🔴 1건은 원본 교재 오식 의심). 전사 에이전트 35개가 스캔 크롭 1520개 목록에서 실제 문항 1570개를 확정했다 — 스캔 배지 OCR 이 놓친 76문항(「번호 빈틈」)을 원본 PDF 쪽 렌더로 추가 전사하고, 문항이 아닌 크롭 26개(유형 라벨 상자 11 · 단원 간지·지시문 조각 4 · 번호를 잘못 읽은 것 11)를 뺐다. 답 출처: 답지 크롭·답지 쪽 렌더 1351 · 해설 크롭 218 · 계산 1. 그림 80개(TikZ 재작도 · 삽화 크롭 · 표 tabular). 전사 review_needed 372건은 `전사-review-needed.json`(대부분 「답 크롭 누락 → 답지 N쪽 렌더로 확정」「말풍선 팁을 hint 에 둠」「라벨 상자 제외」 기록).

## 🔴 사람 확인

- 186-11 (body): 원문 인쇄가 $A^n=\begin{pmatrix} 0 & -32 \\ 1 & 0 \end{pmatrix}$ 인데 $A=\begin{pmatrix} 1 & -2 \\ 0 & 1 \end{pmatrix}$ 이면 $A^n=\begin{pmatrix} 1 & -2n \\ 0 & 1 \end{pmatrix}$ 이라 성립할 수 없고, 답지 113쪽 풀이도 $-2n=-32$, $n=16$(③)으로 푼다 → 원문 오식으로 보이며 전사는 인쇄 그대로 두었다. 실물 교재 확인 뒤 $\begin{pmatrix} 1 & -32 \\ 0 & 1 \end{pmatrix}$ 로 고칠지 결정.

## 🟡 조판 (build.mjs 규칙으로 반영)

- TikZ 그림이 그림 옆 칸(0.4\linewidth)을 넘어 라벨이 잘림(18-04·86-09·86-10·86-12·99-13·147-06·190-03): `renderFigure` 가 TikZ 를 `\resizebox`(칸 폭을 넘을 때만 축소)로 감싼다.
- 첨자·지수 자리의 빈칸 상자가 본문 크기(59-89·59-95·165-27·165-30·165-33·165-36): `_{\blank}`·`^{\blank}` 를 작은 상자(`\text{\scriptsize\blank[0.8em]}`)로 바꿔 쓴다.
- 출처 배지가 앞 문항의 조건 상자·그림 아래변에 닿음(56-54 등): 문항 사이 간격 4mm → 6mm.
- 양끝맞춤 어간 벌어짐·조사 한 글자 줄바꿈·「오.」 고아 줄(15건), 힌트 빈칸 줄바꿈(119-19·122-45·144-17·183-09·183-15·112-15), 행렬 보기 2열 괄호 맞닿음(189-24·191-07·191-11·131-08): 낱장 폭 제약(내용 일치 · 사람 판단).
- 110-01 「꼭 나와요」 태그(빨간 테두리만 있는 문항)는 다른 배치 관례(태그 없음)와 다름 — 사람 판단.

## 답지 정렬

- 스캔 답지 ingest 는 manifest 문항 순서로 해설을 정렬하므로 전사본 기준으로 manifest 를 맞춘 뒤(`sync-manifest-to-bank.mjs` · 추가 76 · 제외 26) 다시 돌렸다: 해설 1454/1570 · 답 줄 1055. 등록 패키지의 답 이미지는 전사본 answer(답지·해설로 확정)를 조판한 것(`build.mjs --export --answers-from-bank`)이라 1570/1570 이고, 해설 크롭이 없는 116문항은 `output/problem-bank/ssen-basic-cm1/검수-필요.md` ①에 있다(필요하면 공통수학2 때처럼 `answer-overrides.json` 으로 채운다).

## 원천

- 검수 JSON: review-01~20.json(ids-NN.txt 가 각 범위) · 전사 review_needed: 전사-review-needed.json
