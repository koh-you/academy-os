# 라이트쎈 공통수학2 LaTeX 전사 검수 요약 · 2026-09-19

전사 정합 검수(원본 스캔 크롭 ↔ 조판본 review/*.png · 크롭이 없거나 잘린 문항은 쪽 이미지 qa/p<pdf쪽>.jpg) — 검수 에이전트 16개(77문항씩) · 확인 1230/1230 · 🔴 0 · 🟡 11(조판).

수치·부호·조건·보기 값·소문항 수의 **내용 불일치는 0건**. 전사 에이전트 28개(K 는 K·KK 로 나눔)가 스캔 크롭 1229개 목록에서 실제 문항 1230개를 확정했다(스캔 배지 OCR 이 놓친 13문항을 쪽 렌더로 추가). 스캔에 05 집합 단원이 없어 0499~0634 번은 이 책에 없다(0635 부터 06 집합의 연산). 전사 review_needed 290건은 `전사-review-needed.json`.

## 🟡 조판 (build.mjs 규칙으로 반영 · 모든 책 공통)

- 0678~0681 공통 지시문의 `$B=\{1,\,2,\,5\}$` 가 `2,` 뒤에서 줄이 바뀌어 `5}` 가 잘림: 수식 안 `\,` 는 math glue 라 줄바꿈 지점이 된다 → `build.mjs` 가 items.json 을 읽을 때 수식 안 `\,` 를 `\mkern3mu`(kern · 줄바꿈 불가)로 바꾼다. 원천 items.json 은 그대로.
- 1381 ④ 보기(한글 24자 + 긴 수식)가 `choicesv` 의 `\mbox` 안에서 잘림: 긴 보기 판정을 수식 길이의 절반을 더해 재는 `visualLength` 로 바꿔 문단 배치로 떨어지게 했다.
- 1018 출처 배지가 공통 지시문 줄과 겹침: 첫 줄에 분수가 있어 배지를 3.5mm 올리는데 낱장 조판(`renderSingle`)은 지시문 뒤 간격을 그만큼 벌리지 않았다 → book.tex 와 같이 지시문 뒤에 `badgeRaiseMm−1.5` mm 를 더한다.
- 1330·1361·1372 양끝맞춤 어간 벌어짐, 1376 배지 잔상(렌더 겹침으로 추정): 낱장 폭 제약 · 내용 일치 · 사람 판단.

## 오판(변경 없음)

- 0189: 「원본에 없는 M 라벨」 — 원본 그림에도 하늘색 M(선분 BC 의 중점)이 있다.
- 1018: 「역함수 그래프 y=−2x 누락」 — 규약(답이 되는 그래프는 그리지 않음)대로 뺀 것. 스캔의 굵은 선·하늘색 선은 답 표시. 1021 은 y=f⁻¹(x) 가 문제의 일부라 그린 것.

## 쪽 번호

- 스캔 manifest 의 `printed_page` 는 pdf 쪽 + 6 인데 05 단원이 빠져 0635 번부터 실제 인쇄 쪽과 16쪽 어긋난다. 전사본 `page` 가 원천이며 `units[].pages` 도 전사본 기준으로 고쳤다(04 60~76 · 06 94~111 · 07 112~133 · 08 136~156 · 09 158~175 · 10 176~189). export 와 `sync-manifest-to-bank.mjs` 가 manifest 의 `printed_page` 를 전사본 page 로 덮어쓴다.
- 첫 병합에서 배치 KK(0635~0648 · 06 단원 개념 쪽)가 04 단원에 붙어 있던 것을 06 단원 맨 앞으로 재병합했다.

## 답지

- 전사본 기준 manifest(추가 13)로 `ingest-scan-answers.mjs --layout lssen --pages 1-136` 을 다시 돌렸다. 등록 패키지의 답 이미지는 전사본 answer 조판(`--answers-from-bank`) 1230/1230.

## 원천

- 검수 JSON: review-01~16.json(ids-NN.txt 가 각 범위) · 전사 review_needed: 전사-review-needed.json
