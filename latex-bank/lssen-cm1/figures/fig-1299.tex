% 1299(본책 181쪽) — 원 위에 같은 간격으로 놓인 8개의 점. 스캔 그림을 TikZ 로 다시 그림. 라벨 없음(지름·직각삼각형 등 답의 근거는 그리지 않음).
\begin{tikzpicture}[line width=0.6pt]
  \def\r{1.2}
  \draw (0,0) circle (\r);
  \foreach \a in {0,45,...,315} { \fill (\a:\r) circle (2.2pt); }
\end{tikzpicture}
