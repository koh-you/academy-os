# 쎈 중3-2 수학 LaTeX 전사 검수 요약 · 2026-09-19

전사 정합 검수(원본 스캔 크롭 ↔ 조판본 review/*.png · 크롭이 없거나 잘린 문항은 쪽 이미지 qa/p<pdf쪽>.jpg) — 검수 에이전트 10개(76문항씩) · 확인 753/753 · 이상 없음 740 · 🔴 0 · 🟡 13(그림 라벨 겹침 9 · 배지 겹침 2 · 보기 잘림 2).

수치·부호·조건·보기 값·소문항 수의 **내용 불일치는 0건**. 에이전트가 🔴로 적은 2건(0152 빗변 「6」이 β로 보임 · 0270 등거리 눈금 누락)은 확인 결과 각각 라벨이 점선 호와 겹쳐 획이 뭉개진 것(전사는 `$6$`)과 눈금이 한 줄로 작게 그려진 것(원문은 ‖)이라 조판 문제로 내렸다. 전사 에이전트 15개가 스캔 크롭 712개 목록에서 실제 문항 753개를 확정했다(스캔 배지 OCR 이 놓친 41문항을 쪽 렌더로 추가). 전사 review_needed 148건은 `전사-review-needed.json`(대부분 「답 크롭 없어 답지 N쪽 렌더로 확정」「□ 는 \blank」「표는 tabular 로 재구성」 기록).

## 🟡 조판 (반영함)

- 그림 라벨이 점선 호·각 표시 호와 겹침 → 라벨 흰 배경(`fill=white`) 또는 자리 이동: 0152(빗변 6) · 0258(12 cm · 6 cm) · 0401(65°·x 가 같은 자리) · 0406(55°·42° 겹침) · 0451(12) · 0577(12°·12°·40° 가 P 근처에 몰림).
- 0332: 접점 라벨 A(위)·B(아래)가 원문과 뒤바뀌어 있었음(4 cm 호는 P–A) → 라벨·호 위치를 원문대로 교체. PA=PB 라 답에는 영향 없음.
- 0270: 중심에서 두 현에 내린 수선의 등거리 표시를 원문처럼 두 줄(‖)로.
- 0124·0125: 공통 표(삼각비 표) 바로 아래 첫 문항의 출처 배지가 표 마지막 행에 겹침 → `build.mjs` 가 그룹 그림 뒤에 4mm 를 둔다(모든 책 공통).
- 0733·0736: 산점도 보기 5개를 3-2 로 놓으면 ③이 낱장 오른쪽 경계에서 잘림 → `choices_layout: "ii"`(2-2-1).
- 0261: 「O–M 연결선 없음」 지적은 오판(선분 있음 · 그림이 작아 그렇게 보임) — 변경 없음.

## 쪽 번호

- 스캔 manifest 의 `printed_page` 는 pdf 쪽 + 고정 offset(6)인데 이 스캔은 단원 간지가 빠져 33쪽부터 실제 인쇄 쪽과 1~10쪽 어긋난다. 전사본 `page`(쪽 이미지에서 읽은 인쇄 쪽)가 원천이며 출처 배지는 이미 맞다. `sync-manifest-to-bank.mjs`·`build.mjs --export` 가 manifest 의 `printed_page` 를 전사본 page 로 덮어쓰도록 고쳤다(라이트쎈 공통수학2 도 05 집합 단원이 스캔에 없어 0635번부터 16쪽 어긋남 — 같은 처리).
- 검수용 ids-NN.txt 의 쪽 이미지 힌트는 고정 offset 으로 만들어 뒤쪽 배치(07·09·10)에서 어긋났고, 에이전트가 실제 쪽을 찾아 대조했다(결과에는 영향 없음).

## 답지

- 전사본 기준 manifest(추가 41 · 제외 0)로 `ingest-scan-answers.mjs --layout lssen` 을 다시 돌렸다. 해설·답 수는 `manifest-answers.json` validation 참고. 등록 패키지의 답 이미지는 전사본 answer 조판(`--answers-from-bank`) 753/753.

## 원천

- 검수 JSON: review-01~10.json(ids-NN.txt 가 각 범위) · 전사 review_needed: 전사-review-needed.json
