# RPM 공통수학1 LaTeX 전사 검수 요약 · 2026-09-17

전사 정합 검수(원본 크롭 ↔ 조판본 review/*.png) — 검수 에이전트 15개(79문항씩) · 확인 1184/1184 · 이상 없음 1055 · 🔴 3 · 🟡 126.

수치·부호·조건·보기 값·소문항 수의 **내용 불일치는 0건**. 원본은 22개정 학생용 텍스트 PDF(회색 워터마크)이며, pdfjs 의 CMap 설정을 고친 뒤 한글·①~⑤·√ 가 모두 읽혀 전사·답지 크롭이 온전했다(전사 review_needed 81건 · `전사-review-needed.json` — 대부분 「보기 상자 크롭은 \bogi 로 전사」「답 크롭에 머리띠 섞임」 같은 기록).

## 🔴 반영

- 0545 (figure): 그림 크롭의 x축 α·β 라벨 위에 워터마크 이메일이 겹침 → figure-overrides.json 에 `whiten: 185`(이 크롭만 회색 190~230 을 흰색으로) 를 두고 재크롭. prepare-text-pdf-bank.mjs 에 문항별 `whiten` 임계값 추가.
- 0703 (body): 원본 크롭 상단이 잘려 대조 불가 → 쪽 렌더로 확인, 조판본 $\frac{x}{3}-\frac{x+4}{2}\le -1$ 이 원문과 일치(수정 없음).
- 0989 (figure): 조건 상자 아래 의자 6개 삽화(장식)가 조판본에 없음 → 자동 크롭이 조건 상자와 합쳐 잡은 것이라 그림을 쓰지 않았다. 문항 내용에는 영향 없음(사람 판단으로 남김).

## 🟡 조판 (build.mjs 규칙으로 반영)

- 출처 배지가 첫 줄의 연립부등식 cases·행렬 pmatrix·분수와 겹침(62건): 배지 올림 높이를 첫 줄 내용에 따라 나눔 — cases·pmatrix 5.5mm · 분수·근호·큰 괄호 3.5mm · 그 밖 1.5mm — 올린 만큼 문항 앞 간격을 더해 앞 문항과 겹치지 않게 함(`badgeRaiseMm`). 재조판 후 0714·1118·0343·0421 확인.
- 줄 끝 끊김·고아 줄(41건): 0046 보기 ④ 긴 등식은 `=` 앞에서 수식을 둘로 나눠 줄바꿈 허용. 나머지(조사 한 글자 줄바꿈 · 「오.」 고아 줄)는 낱장 폭 제약(내용 일치).
- 양끝맞춤 어간 벌어짐(12건)·소문항 2열→세로·보기 배치 차이(6건): 낱장 폭 제약(내용 일치 · 사람 판단).
- 그림 라벨 잘림: fig-g0775-0776(y=f(x) 닫는 괄호)·0480(꼭짓점 C)·0814(y=g(x)) → figure-overrides.json 으로 여백 포함 재크롭(그룹 그림 키 `g<시작>-<끝>` 지원 추가).
- 전사 단계에서 반영한 크롭 재지정: 조립제법 왼쪽 나누는 수(0079·0080·0081·0101), 정사각형 A(0087), 라벨 잘림(0505), 자동 검출이 놓친 점 그림(1050·1051·1057·1058).

## 원천

- 검수 JSON: review-01~15.json · 전사 review_needed: 전사-review-needed.json · 크롭 재지정: ../figure-overrides.json
