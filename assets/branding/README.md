# Branding assets

- `academy-mark.png` — 으뜸수학학원 학사모 아이콘 + 워드마크. 명함 원본(`가로형_명함_완성.zip`
  `001.jpg`)에서 배경(파란색)을 픽셀 단위로 투명 처리해 추출한 뒤, 워드마크 비율을 아이콘 대비
  더 작게 재구성했다. 색상은 짙은 남색(`#111827`)이며 투명 배경. 시험지 워터마크
  (`api/lib/testPaperWatermark.js`)의 기본 로고로 쓰인다.
- 원본 명함 해상도가 낮아(마크 기준 254×229px) 페이지 폭의 큰 비율로 확대하면 다소 흐릿할 수
  있다. 더 고해상도 원본(AI/PDF/SVG)이 생기면 이 파일만 교체하면 된다.
